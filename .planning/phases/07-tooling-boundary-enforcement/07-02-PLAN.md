---
phase: 07-tooling-boundary-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [TOOL-CI, TOOL-LINT, TOOL-DEPCRUISE, TOOL-TEST]
user_setup: []
must_haves:
  truths:
    - "Developer can run `pnpm test` to execute vitest tests (once test files exist)"
    - "Developer can run `pnpm depcruise:check` to run full dependency-cruiser scan with error exit code"
    - "Developer can run `pnpm type-check` to run tsc --noEmit"
    - "GitHub Actions CI runs on every push with 5 steps: install -> lint -> type-check -> depcruise -> test"
    - "Vitest config resolves @/ path alias to ./src/ for imports in test files"
  artifacts:
    - path: "package.json"
      provides: "New scripts, devDependencies, and lint-staged configuration"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with @/ alias resolution"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow with 5-step pipeline"
  key_links:
    - from: "package.json::scripts"
      to: "vitest, depcruise, tsc commands"
    - from: ".github/workflows/ci.yml::steps"
      to: "package.json::scripts"
---

<objective>
Add all tooling devDependencies, scripts, vitest configuration, and GitHub Actions CI workflow.

Purpose: The project gains all the packages and wiring needed for boundary enforcement -- vitest for testing, dependency-cruiser for architecture validation, husky+lint-staged for git hooks, and CI integration.
Output: Updated package.json with new scripts + devDeps + lint-staged config + vitest.config.ts + .github/workflows/ci.yml
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/references/mandatory-initial-read.md
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-tooling-boundary-enforcement/07-CONTEXT.md (D-01, D-06, D-07, D-09, D-11, specifics section)
@.planning/codebase/STACK.md (existing tooling: Biome 2.4.16, TypeScript ^5, pnpm)
@tsconfig.json (paths: @/* -> ./src/* for vitest alias)
@package.json (current scripts and deps -- read before modifying)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update package.json with devDependencies, scripts, and lint-staged config</name>
  <files>package.json</files>
  <read_first>
    - package.json (read current state before editing)
    - CONTEXT.md D-01 (depcruiser v16, vitest), D-07 (husky v9, lint-staged), D-09 (script names), D-11 (vitest latest, no extra libs)
  </read_first>
  <action>
    Modify `package.json` to add, preserving ALL existing fields unchanged:

    **devDependencies** -- add to existing:
    - `"vitest": "latest"` (resolves to latest stable)
    - `"dependency-cruiser": "^16.0.0"` (v16 LTS per D-01)
    - `"husky": "^9.0.0"` (v9 per D-07)
    - `"lint-staged": "latest"` (latest stable)

    **scripts** -- add, do NOT remove existing scripts:
    - `"test": "vitest run"`
    - `"test:watch": "vitest"`
    - `"depcruise": "depcruise src"`
    - `"depcruise:check": "depcruise src --output-type dot | grep -q error"` (exact format per D-09)
    - `"type-check": "tsc --noEmit"` (per D-09)
    - `"prepare": "husky"` (per D-09, auto-run on pnpm install)

    **lint-staged** -- add as top-level key:
    - `"*.{ts,tsx,js,jsx,json}": ["biome check --no-errors-on-unmatched --write"]`

    Do NOT add jsdom, @testing-library, or any deps beyond the 4 listed.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const s = n => !!pkg.scripts?.[n]; const d = n => !!pkg.devDependencies?.[n]; process.exit(['test','test:watch','depcruise','depcruise:check','type-check','prepare'].every(s) && ['vitest','dependency-cruiser','husky','lint-staged'].every(d) && !!pkg['lint-staged'] ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 new scripts present (test, test:watch, depcruise, depcruise:check, type-check, prepare)
    - 4 new devDependencies present (vitest, dependency-cruiser, husky, lint-staged)
    - lint-staged top-level config present with biome check command
    - All existing scripts and deps unchanged
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create vitest.config.ts and .github/workflows/ci.yml</name>
  <files>vitest.config.ts, .github/workflows/ci.yml</files>
  <read_first>
    - tsconfig.json (paths: @/* -> ./src/*)
    - CONTEXT.md D-06 (CI steps), D-08 (vitest config), D-11 (no jsdom)
  </read_first>
  <action>
    Create two files:

    **vitest.config.ts** at project root:
    - Use `defineConfig` from `vitest/config`
    - `resolve.alias` with `@/` mapped to `path.resolve(__dirname, './src')` (import * as path from 'node:path')
    - `test.environment` set to `"node"` (no jsdom needed per D-11)
    - No globals, no plugins, no coverage config

    **`.github/workflows/ci.yml`** -- GitHub Actions workflow:
    - Trigger: `push` on any branch (explicitly `branches: ['**']` per D-06)
    - Job: `ci` running on `ubuntu-latest`
    - Node: 20.x (per D-06)
    - Steps (D-06 order):
      1. Checkout repository
      2. Setup Node.js 20.x with pnpm cache (use `pnpm/action-setup` for pnpm, or `actions/setup-node` with cache: 'pnpm')
      3. `pnpm install --frozen-lockfile`
      4. `pnpm lint`
      5. `pnpm type-check`
      6. `pnpm depcruise:check`
      7. `pnpm test`
    - Workflow name: `CI`
    - Ensure the pnpm version matches the project's local version (10.28.0)
  </action>
    <verify>
      <automated>if (-not (Test-Path "vitest.config.ts" -PathType Leaf)) { exit 1 }; if (-not (Test-Path ".github/workflows/ci.yml" -PathType Leaf)) { exit 1 }</automated>
    </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at project root
    - vitest config uses `defineConfig` with `resolve.alias` for `@/` -> `./src/`
    - vitest test.environment is `"node"`
    - `.github/workflows/ci.yml` exists
    - CI workflow has exactly 7 steps (checkout, setup-node, install, lint, type-check, depcruise, test)
    - CI trigger is on push to any branch
    - Node version is 20.x
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> pnpm install | New devDependencies (vitest, depcruiser, husky, lint-staged) fetched from registry |
| Workflow YAML -> GitHub Actions | CI workflow executed in GitHub runner environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | pnpm install (vitest, depcruiser, husky, lint-staged) | mitigate | Validated by slopcheck + human checkpoint; packages verified on npmjs.com |
| T-07-04 | Tampering | .github/workflows/ci.yml | accept | Workflow file is tracked in git; unauthorized changes visible in PR diff |
| T-07-05 | Denial of Service | CI pipeline | accept | Out of scope for Phase 7; no rate limiting or pipeline cost analysis planned |
</threat_model>

<verification>
- `node -e "require('./package.json')"` succeeds (valid JSON)
- CI workflow file passes GitHub Actions schema validation (check with `node --eval` or manual YAML parse)
- vitest.config.ts has valid TypeScript syntax
</verification>

<success_criteria>
1. package.json has all scripts, deps, and lint-staged config
2. vitest.config.ts created with @/ alias resolution
3. .github/workflows/ci.yml created with 5-step CI pipeline
4. All existing package.json fields preserved unchanged
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/07-tooling-boundary-enforcement/07-02-SUMMARY.md`
</output>
