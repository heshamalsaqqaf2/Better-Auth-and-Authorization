---
phase: 07-tooling-boundary-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
  - .dependency-cruiser.js
autonomous: true
requirements: [TOOL-LINT, TOOL-DEPCRUISE]
user_setup: []
must_haves:
  truths:
    - "Biome noRestrictedImports rules block cross-layer imports during local development, surfacing clear error messages per D-04"
    - "dependency-cruiser rejects any import from Domain to Infrastructure (or other layer violations) as a CI-blocking failure"
    - "Circular dependencies within src/Core/ are detected and reported as errors"
    - "Three architecturally-correct mapper files are NOT falsely flagged by either tool"
  artifacts:
    - path: "biome.json"
      provides: "noRestrictedImports rules in linter.rules.style with 5 layer restriction groups"
      contains: "noRestrictedImports"
    - path: ".dependency-cruiser.js"
      provides: "Forbidden rules per layer (allow-list approach), circular dep detection"
      exports: ["module.exports"]
  key_links:
    - from: "biome.json::linter.rules.style.noRestrictedImports"
      to: "CONTEXT.md D-04/D-05"
      pattern: "noRestrictedImports"
    - from: ".dependency-cruiser.js::forbidden"
      to: "src/Core/Foundations/{Domain,Application,Infrastructure,Presentation}"
      pattern: "pathNot"
---

<objective>
Create the layer boundary enforcement configuration for both Biome (local IDE feedback) and dependency-cruiser (CI gate) that prevents cross-layer import violations.

Purpose: Developers get instant feedback on layer violations during development (Biome) and a hard CI gate (depcruiser) that blocks merges. Both tools share the same allow-list rules from D-02.
Output: Updated biome.json with noRestrictedImports rules + new .dependency-cruiser.js config.
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/references/mandatory-initial-read.md
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 7 section)
@.planning/phases/07-tooling-boundary-enforcement/07-CONTEXT.md (D-01 through D-05, specifics section)
@.planning/STATE.md (current position, accumulated decisions)
@biome.json (current config — add noRestrictedImports to existing linter.rules)

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration needed. -->

**Layer import boundaries (from CONTEXT.md D-02):**
| Layer | Allowed imports from |
|-------|---------------------|
| Kernel | (nothing — zero external dependencies) |
| Base | Kernel only |
| Domain | Kernel, Base only |
| Application | Kernel, Base, Domain only |
| Infrastructure | Kernel, Base, Application only |
| Presentation | Kernel, Application only |

**Mapper files that need allow-listing (D-05):**
1. `src/Core/Foundations/Application/Mappers/domain-to-application-error.mapper.ts` — Application imports from Domain (architecturally correct)
2. `src/Core/Foundations/Infrastructure/Mappers/infrastructure-to-application-error.mapper.ts` — Infrastructure imports from Application
3. `src/Core/Foundations/Presentation/Mappers/application-to-presentation-error.mapper.ts` — Presentation imports from Application

**Current import patterns (from CONTEXT.md code_context):**
- All layers use `@/Core/Kernel/...` for Kernel imports
- Relative `../../Layer/SubModule/file` for cross-layer mapper imports
- Relative `../sub-module/file` for sibling access within same layer

**Biome config location:** linter.rules.style (add noRestrictedImports here; keep existing rules.recommended and domains unchanged)
**depcruise config format:** CommonJS module.exports (.dependency-cruiser.js, not .cjs or .mjs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update biome.json with noRestrictedImports rules</name>
  <files>biome.json</files>
  <read_first>
    - biome.json (read current state — has recommended rules, next/react domains)
    - CONTEXT.md D-04 and D-05 for scope and allow-list requirements
  </read_first>
  <action>
    Add `noRestrictedImports` to `linter.rules.style` as an error-level rule with:

    Create 5 layer restriction groups (one per layer) under `linter.rules.style.noRestrictedImports.options.paths`. Each group:
    - Blocks all non-allowed import source prefixes for files in that layer, using the import boundary table above
    - Uses verbose error messages in the format per D-04: `"[source layer name] cannot import from [target layer name]. Allowed imports: [list]. See docs/Architecture.md for layer rules."`
    - Covers both `@/Core/Foundations/[Layer]/...` alias imports AND relative path patterns

    Add file-glob allow-lists for the 3 mapper files listed in D-05 so they DO NOT trigger restrictions for their architecturally-correct imports.

    Keep all existing linter configuration unchanged (recommended rules, suspicious.noUnknownAtRules:off, next/react domains).
    Do NOT change formatter, vcs, files, or assist sections.
  </action>
  <verify>
    <automated>npx biome lint --config-path=biome.json --no-errors-on-unmatched 2>&1 ; if ($?) { "config validates" }</automated>
  </verify>
  <acceptance_criteria>
    - biome.json validates (no parse errors) with `npx biome --version` + config check
    - `linter.rules` has a `style` key with `noRestrictedImports` containing exactly 5 restriction groups
    - Error messages contain specific layer names and the "Allowed imports:" phrase from D-04
    - The 3 mapper files from D-05 are referenced by glob pattern in the config (not per-line disable comments)
    - All existing config fields (formatter, vcs, files, linter.domains, assist) remain unchanged
    - Relative import patterns AND `@/Core/` alias patterns are both covered for each restriction group
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .dependency-cruiser.js with per-layer allow-lists</name>
  <files>.dependency-cruiser.js</files>
  <read_first>
    - CONTEXT.md D-01 through D-03 (tooling stack, enforcement approach, circular deps)
    - CONTEXT.md specifics section (allow-list regex patterns, internal enforcement scope)
  </read_first>
  <action>
    Create `.dependency-cruiser.js` (CommonJS module.exports) with:

    1. **Root options:**
       - `exclude` pattern for `node_modules`, `.next/`, and test artifact dirs
       - No `tsConfig` option needed (depcruise v16 auto-resolves TS paths)
       - No `webpackConfig` option (depcruise handles tsconfig paths natively)

    2. **Forbidden rules** (allow-list approach using `from.path` + `to.pathNot`):
       - Create 5+ forbidden rules, one per layer, using the import boundary table
       - For each layer: `from.path` matches files in that layer, `to.pathNot` lists *only* the allowed targets
       - Pattern for each rule: `{ name, severity: "error", comment, from: { path }, to: { pathNot: [...] } }`
       - Apply a top-level scope filter so rules only apply within `src/Core/{Kernel,Foundations}/` — app code (`src/app/`, `src/Shared/`, `src/Lib/`) can import from Core freely (D-02 internal-layers-only)
       - All 6 layers covered: Kernel, Base, Domain, Application, Infrastructure, Presentation

    3. **Circular dependency detection rule (D-03):**
       - One `forbidden` rule: `from: { path: "^src/Core/" }` with `to: { circular: true }`
       - Severity: `"error"` — any cycle = failure
       - No exceptions or exclusion paths

    4. **Allowed module rule:**
       - Explicitly allow app-level modules to import Core: add a rule or use `allowed` or `allowedSeverity` so non-Core files are not flagged

    Use the regex path patterns from CONTEXT.md specifics section for the allow-list structure.
    Per D-01: no madge config needed — depcruiser covers circular deps.
  </action>
  <verify>
    <automated>node -e "const config = require('./.dependency-cruiser.js'); const rules = config.forbidden || []; console.log('Rules:', rules.length); process.exit(rules.length >= 6 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.dependency-cruiser.js` exists and is valid CommonJS (requires without error)
    - Contains exactly 1 circular dependency rule targeting `src/Core/`
    - Contains at least 5 layer boundary rules (one per Foundation layer + Kernel)
    - Each layer boundary rule uses `pathNot` with the correct allowed-target list from D-02
    - All rules scoped to `src/Core/` (app code excluded per internal-layers-only)
    - No rules reference madge (D-01)
    - `severity: "error"` on all rules (violations are CI-blocking)
    - No exceptions for any layer (rules are symmetric per D-04)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → Biome config | Biome noRestrictedImports can be disabled or bypassed by modifying biome.json |
| Local dev → depcruiser config | depcruiser config can be modified to weaken enforcement locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | biome.json | mitigate | CI runs depcruise independently (Plan 02); local Biome bypass does not prevent CI gate |
| T-07-02 | Tampering | .dependency-cruiser.js | mitigate | GitHub Actions uses the committed config; git diff on PRs reveals config changes |
| T-07-SC | Tampering | npm install (Biome is already installed; no new packages in this plan) | accept | No new packages installed in this plan; Biome 2.4.16 already in pnpm-lock |
</threat_model>

<verification>
- `node -e "require('./.dependency-cruiser.js')"` succeeds without errors
- `npx biome lint --no-errors-on-unmatched` completes without errors (linter validates)
- Both configs produce no false positives on the current codebase (all current imports are architecturally correct per CONTEXT.md)
</verification>

<success_criteria>
1. biome.json has noRestrictedImports with 5 layer restriction groups and 3 mapper allow-lists
2. .dependency-cruiser.js exists with 6+ forbidden rules (5 layers + circular)
3. Current codebase still lints/type-checks clean (no false positives)
4. Both configs are valid and parse correctly
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/07-tooling-boundary-enforcement/07-01-SUMMARY.md`
</output>
