---
phase: 07-tooling-boundary-enforcement
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - .husky/pre-commit
  - .husky/pre-push
  - src/Core/Foundations/Presentation/__tests__/serialization-safety.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [TOOL-HOOKS, TOOL-TEST, TOOL-DEPCRUISE]
user_setup: []
must_haves:
  truths:
    - "Serialization safety tests pass: no prototype leak, JSON round-trip preserves shape, all 5 variants covered, _tag preserved after serialize/deserialize"
    - "Husky pre-commit hook runs lint-staged (Biome lint + format on staged files)"
    - "Husky pre-push hook runs dependency-cruiser on changed files"
    - "pnpm install completes successfully installing all devDependencies"
    - "Husvy prepare hook auto-runs on pnpm install (verify .husky/_/ directory exists)"
  artifacts:
    - path: "src/Core/Foundations/Presentation/__tests__/serialization-safety.test.ts"
      provides: "Serialization safety tests with 4 coverage areas"
      contains: "describe, it, expect from vitest"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook executing lint-staged"
      permissions: "executable"
    - path: ".husky/pre-push"
      provides: "Pre-push hook executing dependency-cruiser on changed files"
      permissions: "executable"
  key_links:
    - from: ".husky/pre-commit"
      to: "package.json::lint-staged"
      via: "pnpm lint-staged"
    - from: ".husky/pre-push"
      to: "pnpm depcruise:changed"
      via: "pnpm depcruise:changed"
    - from: "serialization-safety.test.ts"
      to: "src/Core/Foundations/Presentation/Errors/presentation-error.types.ts"
      via: "import from @/Core/Foundations/Presentation/..."
---

<objective>
Install all tooling dependencies, create git hooks for local enforcement, and write serialization safety tests.

Purpose: Developers get pre-commit/pre-push hooks that enforce layer boundaries locally before CI catches issues. Serialization safety tests verify that Presentation DU types remain plain objects after JSON serialize/deserialize -- no class instances cross the boundary.
Output: .husky/pre-commit + .husky/pre-push hooks, serialization-safety.test.ts with 4 coverage areas, pnpm install completed.
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/references/mandatory-initial-read.md
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-tooling-boundary-enforcement/07-CONTEXT.md (D-07, D-08, D-09, D-10, D-11, specifics section)
@.planning/ROADMAP.md (Phase 7 success criteria)
@package.json (scripts section -- confirm test and prepare scripts exist from Plan 02)
@tsconfig.json (@/* -> ./src/* for vitest alias resolution)
@src/Core/Foundations/Presentation/Errors/presentation-error.types.ts (5 DU variants for test reference)
@src/Core/Foundations/Presentation/Errors/presentation-error.ts (factory functions to import in tests)
@src/Core/Foundations/Presentation/Results/presentation-result.ts (PresentationResult types to test)

<interfaces>
<!-- Key types and imports the executor needs. No codebase exploration needed. -->

**Import paths for test file (via @/ alias or relative):**
```typescript
// PresentationError variants (5 types):
import type { PresentationError } from "@/Core/Foundations/Presentation/Errors/presentation-error.types";
import type { ValidationErrorShape, NotFoundErrorShape, AuthorizationErrorShape, SystemErrorShape, NetworkErrorShape } from "@/Core/Foundations/Presentation/Errors/presentation-error.types";

// Factory functions:
import { createValidationError, createNotFoundError, createAuthorizationError, createSystemError, createNetworkError } from "@/Core/Foundations/Presentation/Errors/presentation-error";

// PresentationResult (for prototype leak test):
import { successResult, failureResult } from "@/Core/Foundations/Presentation/Results/presentation-result";
import type { PresentationResult, PresentationSuccess, PresentationFailure } from "@/Core/Foundations/Presentation/Results/presentation-result";
```

**DU Variants (for test values):**
| Variant | Factory | Key fields |
|---------|---------|------------|
| ValidationError | createValidationError(code, fieldErrors) | _tag: "ValidationError", errorCode, fieldErrors: Record<string, string[]> |
| NotFoundError | createNotFoundError(code, message, suggestedAction?) | _tag: "NotFoundError", errorCode, userMessage, suggestedAction?: string |
| AuthorizationError | createAuthorizationError(code, message) | _tag: "AuthorizationError", errorCode, userMessage |
| SystemError | createSystemError(code, message, severity) | _tag: "SystemError", errorCode, userMessage, severity: "warning"\|"error"\|"critical" |
| NetworkError | createNetworkError(code, message, retryable) | _tag: "NetworkError", errorCode, userMessage, retryable: boolean |

**PresentationResult (for prototype leak test):**
- successResult(data, operationId) -> { _tag: "Success", data, operationId }
- failureResult(error, operationId) -> { _tag: "Failure", error, operationId }

**Test coverage areas (D-08):**
1. No prototype leak -- Object.getPrototypeOf(result) === Object.prototype
2. JSON round-trip -- JSON.stringify -> JSON.parse preserves object shape for all 5 variants
3. All 5 variants covered -- each _tag variant tested individually
4. _tag discriminant preservation -- _tag is correct after serialization/deserialization

**Package scripts available (from Plan 02):**
- `pnpm test` = `vitest run`
- `pnpm prepare` = `husky` (auto-runs on install)

**Husky v9 hook format:**
Husky v9 uses `.husky/` directory with shell scripts. Hooks are plain files named after the hook type, without `.sh` extension, in the `.husky/` directory. First line must be `#!/usr/bin/env sh` followed by `. "$(dirname "$0")/_/husky.sh"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run pnpm install and create .husky git hooks</name>
  <files>package.json, .husky/pre-commit, .husky/pre-push, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify scripts and devDependencies from Plan 02 exist)
    - CONTEXT.md D-07 (husky v9, pre-commit lint-staged, pre-push depcruise)
    - CONTEXT.md D-09 (prepare script already in package.json, depcruise:check defined)
  </read_first>
  <action>
    Run two sub-steps sequentially:

    1. **pnpm install** -- run `pnpm install` (not frozen-lockfile since deps are new). This installs all 4 new devDependencies and runs the `prepare` script (husky sets up `.husky/_/` directory).

    2. **Create .husky hook files** after pnpm install completes:

       **`.husky/pre-commit`**:
       - Shebang: `#!/usr/bin/env sh`
       - Husky v9 bootstrap: `. "$(dirname "$0")/_/husky.sh"`
       - Command: `pnpm lint-staged`
       - File must be executable (use `git add --chmod=+x` or `icacls` on Windows)

       **`.husky/pre-push`**:
       - Shebang: `#!/usr/bin/env sh`
       - Husky v9 bootstrap: `. "$(dirname "$0")/_/husky.sh"`
       - Command: `pnpm depcruise:check`
       - File must be executable

       The `depcruise:changed` script is NOT defined in D-09; use `depcruise:check` instead for pre-push since it runs a full scan on changed files context -- per the agent's discretion and D-07's intent of catching violations before push.
  </action>
    <verify>
      <automated>if (-not (Test-Path ".husky/pre-commit" -PathType Leaf) -or -not (Test-Path ".husky/pre-push" -PathType Leaf) -or -not ((Get-Content ".husky/pre-commit" | Select-String -Quiet "lint-staged"))) { exit 1 }</automated>
    </verify>
  <acceptance_criteria>
    - `pnpm install` completes without errors (exit code 0)
    - `.husky/pre-commit` exists with `pnpm lint-staged` command
    - `.husky/pre-push` exists with `pnpm depcruise:check` command
    - Both hook files start with proper shebang and husky v9 bootstrap line
    - `.husky/_/husky.sh` exists (husky installed itself during prepare)
    - All 4 devDependencies listed in `pnpm list --dev` output
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create serialization-safety.test.ts with 4 coverage areas</name>
  <files>src/Core/Foundations/Presentation/__tests__/serialization-safety.test.ts</files>
  <read_first>
    - src/Core/Foundations/Presentation/Errors/presentation-error.types.ts (5 DU variant type definitions)
    - src/Core/Foundations/Presentation/Errors/presentation-error.ts (5 factory functions)
    - src/Core/Foundations/Presentation/Results/presentation-result.ts (successResult, failureResult factories and types)
    - CONTEXT.md D-08 (4 coverage areas: no prototype leak, JSON round-trip, all 5 variants, _tag preservation)
  </read_first>
  <action>
    Create the `__tests__` directory and `serialization-safety.test.ts` file inside it.

    The test file must use `import { describe, it, expect } from "vitest"` (explicit imports, no globals).

    Import factory functions and types using `@/Core/Foundations/Presentation/...` paths (vitest config resolves @/).

    **Test structure -- 4 `describe` blocks:**

    1. **"no prototype leak"**:
       - Test that EACH of the 5 PresentationError variants has `Object.getPrototypeOf(result) === Object.prototype`
       - Test that `successResult` and `failureResult` return values have `Object.getPrototypeOf(result) === Object.prototype`
       - Use `it.each` or individual `it()` calls for each variant

    2. **"JSON round-trip preserves shape"**:
       - For each of the 5 variants: create an instance, `JSON.stringify` it, `JSON.parse` the result
       - Verify the parsed object has the same keys and primitive values as the original
       - Use `it.each` with the 5 variants as test cases
       - For each parsed result, verify: all expected keys exist, no extra keys, primitive values match

    3. **"all 5 variants"**:
       - Verify each variant has the correct `_tag` string value
       - Test specific variant fields: ValidationError has `fieldErrors`, NotFoundError has `userMessage` and optional `suggestedAction`, SystemError has `severity`, NetworkError has `retryable`
       - This is a structural coverage test ensuring all 5 variants from the type file are instantiable

    4. **"_tag discriminant preservation"**:
       - For each variant, serialize with `JSON.stringify`, deserialize with `JSON.parse`
       - Verify the `_tag` property equals the original value after round-trip
       - This ensures the discriminant survives RSC serialization (per Architecture.md Serialization Boundary Rule)

    Use `it.each` with descriptive test names. Each test case should be individually identifiable.
    Do NOT test PresentationResult in JSON round-trip (it contains operationId which is dynamic).
    Add a comment at the top: "Phase 7: Serialization safety tests -- verifies boundary enforcement per CONTEXT.md D-08"
  </action>
  <verify>
    <automated>pnpm test -- --run 2>&1 | Select-String -Pattern "Tests.*(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/Core/Foundations/Presentation/__tests__/serialization-safety.test.ts`
    - `pnpm test` passes with no failures
    - All 4 coverage areas from D-08 are represented as separate `describe` blocks
    - All 5 PresentationError variants are tested in each relevant coverage area
    - `Object.getPrototypeOf()` tests pass for all variants (no prototype leak)
    - JSON round-trip tests pass for all 5 variants (shape preserved)
    - `_tag` value survives serialization/deserialization for all 5 variants
    - No external test libraries imported beyond `vitest`
    - `npx tsc --noEmit` still passes with no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> pnpm install | New packages fetched from registry -- supply chain risk |
| Serialization boundary -> test | Tests verify that no class instances cross the Presentation boundary |
| Git hook scripts -> developer environment | .husky scripts execute on every commit/push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | pnpm install (vitest, depcruiser, husky, lint-staged) | mitigate | Run `pnpm audit` after install; record package names and versions for verification |
| T-07-06 | Tampering | .husky/pre-commit, .husky/pre-push | accept | Hook scripts are tracked in git; unauthorized changes visible in commit diff. CI also independently enforces rules |
| T-07-07 | Information Disclosure | Serialization safety tests | accept | Tests run on in-memory objects; no sensitive data involved |
</threat_model>

<verification>
- `pnpm install` exits 0 (all devDeps installed)
- `pnpm list --dev --depth 0` shows vitest, dependency-cruiser, husky, lint-staged
- `.husky/pre-commit` exists and is executable
- `.husky/pre-push` exists and is executable
- `pnpm test` passes (exit code 0)
- `npx tsc --noEmit` passes (exit code 0) -- no TS errors from test file
- `npx biome check` passes (no lint errors from test file)
</verification>

<success_criteria>
1. All 4 devDependencies installed successfully
2. Husky pre-commit hook calls lint-staged
3. Husky pre-push hook calls depcruise:check
4. All serialization safety tests pass (4 coverage areas, all 5 variants)
5. TypeScript compilation passes -- no errors
6. Biome lint passes -- no errors from new files
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/07-tooling-boundary-enforcement/07-03-SUMMARY.md`
</output>
