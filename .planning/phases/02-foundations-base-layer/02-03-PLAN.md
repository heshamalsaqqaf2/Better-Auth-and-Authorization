---
phase: 02-foundations-base-layer
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/Core/Foundations/Base/Factories/validation-error.ts
  - src/Core/Foundations/Base/Factories/correlation-id-safe.factory.ts
  - src/Core/Foundations/Base/Factories/error-code-safe.factory.ts
  - src/Core/Foundations/Base/Factories/operation-id-safe.factory.ts
  - src/Core/Foundations/Base/Factories/index.ts
autonomous: true
requirements: [BASE-01, BASE-02]

must_haves:
  truths:
    - "Developer can create ValidationError lightweight interface objects via createValidationError(message)"
    - "Developer can create CorrelationId safely (no throw) via createCorrelationIdSafe(), receiving err(ValidationError) on invalid input"
    - "Developer can create ErrorCode safely via createErrorCodeSafe(), receiving err(ValidationError) on invalid input"
    - "Developer can create OperationId safely via createOperationIdSafe(), receiving err(ValidationError) on invalid input"
    - "Original Phase 1 throwing factories (createCorrelationId, createErrorCode, createOperationId) remain unchanged"
    - "Safe factories wrap the Phase 1 throwing factory in try/catch, returning ok() on success and err(createValidationError(...)) on throw"
  artifacts:
    - path: "src/Core/Foundations/Base/Factories/validation-error.ts"
      provides: "ValidationError interface + createValidationError factory"
      min_lines: 20
    - path: "src/Core/Foundations/Base/Factories/correlation-id-safe.factory.ts"
      provides: "Result-wrapped safe factory for CorrelationId"
      min_lines: 15
    - path: "src/Core/Foundations/Base/Factories/error-code-safe.factory.ts"
      provides: "Result-wrapped safe factory for ErrorCode"
      min_lines: 15
    - path: "src/Core/Foundations/Base/Factories/operation-id-safe.factory.ts"
      provides: "Result-wrapped safe factory for OperationId"
      min_lines: 15
    - path: "src/Core/Foundations/Base/Factories/index.ts"
      provides: "Barrel re-exporting all factory exports"
      min_lines: 6
  key_links:
    - from: "Factories/correlation-id-safe.factory.ts"
      to: "Kernel/Primitives/Types/correlation-id.type.ts"
      via: "wraps createCorrelationId in try/catch"
      pattern: "createCorrelationId"
    - from: "Factories/*.factory.ts"
      to: "Abstracts/result-base.ts"
      via: "return type ok()/err() from Base result types"
      pattern: "ok\\(|err\\("
    - from: "Factories/validation-error.ts"
      to: "Kernel/Contracts/Base/error-base.contract.ts"
      via: "structurally matches ErrorBase interface"
      pattern: "interface ValidationError"
---

<objective>
Create ValidationError interface and safe Result-wrapped factory functions for the Kernel's throwing branded type factories.

Purpose: Provide non-throwing alternatives to the Phase 1 branded type factories (createCorrelationId, createErrorCode, createOperationId) that return typed Result monads instead of throwing, enabling ergonomic error handling without try/catch at every call site. Original factories remain untouched per D-26.
Output: Factories/ directory with validation-error.ts, three safe factory files, and index.ts barrel
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/Core/Kernel/Primitives/Types/correlation-id.type.ts
@src/Core/Kernel/Primitives/Types/error-code.type.ts
@src/Core/Kernel/Primitives/Types/operation-id.type.ts
@src/Core/Kernel/Contracts/Base/error-base.contract.ts
@src/Core/Foundations/Base/Abstracts/result-base.ts
@src/Core/Foundations/Base/Abstracts/error-base.ts
@src/Core/Kernel/Contracts/Base/result-base.contract.ts

<interfaces>
From src/Core/Kernel/Primitives/Types/correlation-id.type.ts:
```typescript
export type CorrelationId = Brand<string, 'CorrelationId'>;
export function createCorrelationId(value: string): CorrelationId;
```
(Identical pattern for ErrorCode, OperationId)

From src/Core/Kernel/Contracts/Base/error-base.contract.ts:
```typescript
export interface ErrorBase {
  readonly code: string;
  readonly message: string;
  readonly timestamp: Date;
  readonly layer: LayerType;
  readonly cause?: ErrorBase;
  isRecoverable(): boolean;
  getSeverity(): Severity;
  toJSON(): Record<string, unknown>;
}
```

From src/Core/Foundations/Base/Abstracts/result-base.ts (once Plan 01 completes):
- ok<T>(data: T): Success<T>
- err<E extends ErrorBase>(error: E): Failure<never, E>
- Success<T>, Failure<T, E>, ResultBase<T, E>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ValidationError interface and createValidationError factory</name>
  <files>
    - src/Core/Foundations/Base/Factories/validation-error.ts
  </files>
  <read_first>
    - src/Core/Kernel/Contracts/Base/error-base.contract.ts (structural template for ValidationError)
    - src/Core/Kernel/Primitives/Enums/layer-type.enum.ts (LayerType values)
    - src/Core/Kernel/Primitives/Enums/severity.enum.ts (Severity values)
  </read_first>
  <action>
    Per D-27: Create a lightweight `ValidationError` interface and a `createValidationError()` factory function in `Factories/validation-error.ts`.

    The `ValidationError` interface is NOT a class — it is an interface that structurally satisfies the Kernel `ErrorBase` contract (duck-typing compatible). It has the same shape as `ErrorBase` but is a standalone interface.

    The interface fields:
    - `readonly code: string`
    - `readonly message: string`
    - `readonly timestamp: Date`
    - `readonly layer: typeof LayerType[keyof typeof LayerType]` (should use a string literal — use `'domain'`, but actually use the `LayerType` const values. Since ValidationError is meant as a generic validation error at the Base layer, use `LayerType.DOMAIN` — wait, that doesn't make sense. Let me think about this.)

    Actually, a ValidationError from the Base layer doesn't belong to any specific Domain. It could use a general layer value. Looking at the context, the `createValidationError` factory creates a duck-typed object that structurally satisfies ErrorBase. The layer should probably be something meaningful.

    Looking at the error contract, `layer` is a `LayerType`. For a validation error at the Base layer, we should set it to a reasonable value. Since Base layer is the foundation, not a specific domain, we could set it to a general purpose. But there's no `base` or `core` layer type. The existing options are: `domain`, `application`, `infrastructure`, `presentation`.

    For a validation error factory at the Base level, I'll use a fixed appropriate layer. The user said "lightweight interface only; concrete class in Phase 3" in the domain section. Given that this is a generic validation error at the Foundation Base level, `LayerType.DOMAIN` makes sense since validation rules typically apply at the domain boundary. But let me not overthink this — the agent's discretion allows the exact formatting.

    The `createValidationError(message: string): ValidationError` factory:
    - Creates a plain object satisfying the interface
    - `code` = `'VALIDATION_ERROR'` (constant string)
    - `message` = the `message` parameter
    - `timestamp` = `new Date()`
    - `layer` = a sensible default like `LayerType.DOMAIN` (the agent's discretion per guidelines; the exact value is not locked in decisions)
    - `isRecoverable()` returns `false`
    - `getSeverity()` returns `Severity.ERROR`
    - `toJSON()` returns a serializable Record with code, message, timestamp ISO string, layer, severity, recoverable — flat structure, no circularity handling needed for a simple factory object.

    Import `LayerType` and `Severity` from Kernel enums.
    No need to implement the ErrorBase contract — just structurally match it.

    Export both the interface and the factory function.
  </action>
  <acceptance_criteria>
    - `ValidationError` interface is exported with code (string), message (string), timestamp (Date), layer (LayerType), isRecoverable (function), getSeverity (function), toJSON (function) members
    - `createValidationError(message: string): ValidationError` function is exported
    - Returned object's `toJSON()` includes code, message, timestamp, layer, severity, recoverable keys
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>ValidationError interface + createValidationError factory compiles cleanly under npx tsc --noEmit</done>
</task>

<task type="auto">
  <name>Task 2: Implement three safe Result-wrapped factory functions</name>
  <files>
    - src/Core/Foundations/Base/Factories/correlation-id-safe.factory.ts
    - src/Core/Foundations/Base/Factories/error-code-safe.factory.ts
    - src/Core/Foundations/Base/Factories/operation-id-safe.factory.ts
  </files>
  <read_first>
    - src/Core/Kernel/Primitives/Types/correlation-id.type.ts (wraps createCorrelationId)
    - src/Core/Kernel/Primitives/Types/error-code.type.ts (wraps createErrorCode)
    - src/Core/Kernel/Primitives/Types/operation-id.type.ts (wraps createOperationId)
    - src/Core/Foundations/Base/Factories/validation-error.ts (uses createValidationError)
    - src/Core/Foundations/Base/Abstracts/result-base.ts (uses ok/err/ResultBase types)
  </read_first>
  <action>
    Per D-25, D-26, D-28: Create three files, each wrapping one Phase 1 throwing factory in a try/catch and returning `ok()` on success / `err(createValidationError(...))` on failure.

    Per D-28: Return type is `Result<BrandType, ValidationError>` — which maps to `ResultBase<BrandType, ValidationError>` (the abstract class type). Use the concrete `ok()` and `err()` from the Abstracts layer.

    **correlation-id-safe.factory.ts**:
    - Import `createCorrelationId`, `CorrelationId` from Kernel types (path: `../../../Kernel/Primitives/Types/correlation-id.type`)
    - Import `ok`, `err`, `ResultBase` from `../Abstracts/result-base`
    - Import `createValidationError` from `./validation-error`
    - Import `ValidationError` type from `./validation-error`
    - Signature: `export function createCorrelationIdSafe(value: string): ResultBase<CorrelationId, ValidationError>`
    - Body: `try { return ok(createCorrelationId(value)); } catch (e) { return err(createValidationError((e as Error).message)); }`

    **error-code-safe.factory.ts**:
    - Import `createErrorCode`, `ErrorCode` from Kernel types (path: `../../../Kernel/Primitives/Types/error-code.type`)
    - Import `ok`, `err`, `ResultBase` from Abstracts
    - Import `createValidationError`, `ValidationError` from `./validation-error`
    - Signature: `export function createErrorCodeSafe(value: string): ResultBase<ErrorCode, ValidationError>`
    - Body: same try/catch pattern

    **operation-id-safe.factory.ts**:
    - Import `createOperationId`, `OperationId` from Kernel types (path: `../../../Kernel/Primitives/Types/operation-id.type`)
    - Import `ok`, `err`, `ResultBase` from Abstracts
    - Import `createValidationError`, `ValidationError` from `./validation-error`
    - Signature: `export function createOperationIdSafe(value: string): ResultBase<OperationId, ValidationError>`
    - Body: same try/catch pattern

    In each catch block, extract the error message. The Phase 1 factories throw `TypeError` with messages like "CorrelationId must be a non-empty string". Pass that message to `createValidationError()` so the error message is preserved.

    Note: `err()` requires its argument to satisfy `ErrorBase` (the Kernel contract interface). `ValidationError` structurally matches `ErrorBase` since it has all the required fields, so it should satisfy the type constraint without explicit implements. If the compiler complains about structural compatibility, the return type on `err()` is `Failure<never, ValidationError>` which extends `ResultBase<never, ValidationError>` — the `E` constraint inherits from the abstract class declaration.
  </action>
  <acceptance_criteria>
    - `createCorrelationIdSafe(value: string): ResultBase<CorrelationId, ValidationError>` exported from correlation-id-safe.factory.ts
    - `createErrorCodeSafe(value: string): ResultBase<ErrorCode, ValidationError>` exported from error-code-safe.factory.ts
    - `createOperationIdSafe(value: string): ResultBase<OperationId, ValidationError>` exported from operation-id-safe.factory.ts
    - Each safe factory returns `ok(brandedValue)` on valid input (non-empty string)
    - Each safe factory returns `err(validationError)` on invalid input (empty string)
    - Original Phase 1 factories are NOT modified
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>All three safe factory functions compile cleanly under npx tsc --noEmit</done>
</task>

<task type="auto">
  <name>Task 3: Create Factories barrel index</name>
  <files>
    - src/Core/Foundations/Base/Factories/index.ts
  </files>
  <read_first>
    - src/Core/Foundations/Base/Factories/validation-error.ts (to extract exports)
    - src/Core/Foundations/Base/Factories/correlation-id-safe.factory.ts
    - src/Core/Foundations/Base/Factories/error-code-safe.factory.ts
    - src/Core/Foundations/Base/Factories/operation-id-safe.factory.ts
  </read_first>
  <action>
    Create `Factories/index.ts` as a barrel file re-exporting all public symbols from all four factory files:
    - `ValidationError` (type) and `createValidationError` from `./validation-error`
    - `createCorrelationIdSafe` from `./correlation-id-safe.factory`
    - `createErrorCodeSafe` from `./error-code-safe.factory`
    - `createOperationIdSafe` from `./operation-id-safe.factory`

    Use type re-exports for the `ValidationError` interface (it's a type-only export).
  </action>
  <acceptance_criteria>
    - `Factories/index.ts` re-exports `ValidationError`, `createValidationError`, `createCorrelationIdSafe`, `createErrorCodeSafe`, `createOperationIdSafe`
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Factories/index.ts barrel compiles cleanly under npx tsc --noEmit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Safe factories call Phase 1 throwing factories internally — TypeErrors are caught and converted to typed Results.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Base-04 | Tampering | Safe factory try/catch | mitigate | catch clause is scoped and converts only TypeError to ValidationError; unexpected errors still propagate |
| T-Base-05 | Information Disclosure | ValidationError.message | accept | Message originates from Phase 1 TypeError message — by design informative; no sensitive data |
| T-Base-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external dependencies — no package install needed |
</threat_model>

<verification>
All tasks must pass: `npx tsc --noEmit` compiles with no errors.
</verification>

<success_criteria>
- ValidationError interface and createValidationError factory exist
- Three safe factory functions (CorrelationId, ErrorCode, OperationId) with try/catch wrapping Phase 1 throwing factories
- All safe factories return Result<BrandType, ValidationError> — ok on valid, err on invalid
- Original Phase 1 factories remain unmodified
- Factories/index.ts barrel re-exports all public symbols
- `npx tsc --noEmit` passes
</success_criteria>

<output>
Create `.planning/phases/02-foundations-base-layer/02-03-SUMMARY.md` when done
</output>
