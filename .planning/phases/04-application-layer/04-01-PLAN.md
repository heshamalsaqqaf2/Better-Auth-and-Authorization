---
phase: 04-application-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Core/Foundations/Application/Contracts/application-error.contract.ts
  - src/Core/Foundations/Application/Contracts/request-context.contract.ts
autonomous: true
requirements:
  - APP-01
user_setup: []

must_haves:
  truths:
    - "ApplicationErrorContract interface defines operationName: string, correlationId: CorrelationId, userId?: string atop ErrorBase"
    - "RequestContext interface defines correlationId: CorrelationId and userId?: string"
    - "Stub/empty files from scaffolding phase are removed (no dead code)"
  artifacts:
    - path: "src/Core/Foundations/Application/Contracts/application-error.contract.ts"
      provides: "ApplicationErrorContract interface"
      min_lines: 12
    - path: "src/Core/Foundations/Application/Contracts/request-context.contract.ts"
      provides: "RequestContext interface"
      min_lines: 5
  key_links:
    - from: "Contracts/application-error.contract.ts"
      to: "Kernel/Contracts/Base/error-base.contract.ts"
      via: "extends ErrorBase"
      pattern: "extends ErrorBase"
    - from: "Contracts/request-context.contract.ts"
      to: "Kernel/Primitives/Types/correlation-id.type.ts"
      via: "imports CorrelationId"
      pattern: "CorrelationId"
---

<objective>
Create the Application Layer contract interfaces and remove scaffolding stubs.

**Purpose:** Establish `ApplicationErrorContract` (the contract `ApplicationError` will implement in Plan 02) and `RequestContext` (for correlation propagation via method arguments, per D-14/D-15). Delete scaffold files whose purpose contradicts locked decisions (D-12: pure type alias needs no contract/validator/type-guard) and empty subdirectory stubs.

**Output:**
- `Contracts/application-error.contract.ts` — `ApplicationErrorContract` interface
- `Contracts/request-context.contract.ts` — `RequestContext` interface
- 3 stub files deleted from `Results/`
- 4 subdirectories cleared from `Errors/Specific/`
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-application-layer/04-CONTEXT.md
@.planning/phases/04-application-layer/04-PATTERNS.md
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/Core/Kernel/Contracts/Base/error-base.contract.ts:
```typescript
export interface ErrorBase {
  readonly code: string;
  readonly message: string;
  readonly timestamp: Date;
  readonly layer: LayerType;
  readonly cause?: ErrorBase;
  isRecoverable(): boolean;
  getSeverity(): Severity;
  toJSON(): Record<string, unknown>;
}
```

From src/Core/Kernel/Primitives/Types/correlation-id.type.ts:
```typescript
export type CorrelationId = Brand<string, 'CorrelationId'>;
export function isCorrelationId(value: unknown): value is CorrelationId;
```

From src/Core/Kernel/Primitives/Enums/layer-type.enum.ts:
```typescript
export const LayerType = { DOMAIN: 'domain', APPLICATION: 'application', INFRASTRUCTURE: 'infrastructure', PRESENTATION: 'presentation' } as const;
export type LayerType = (typeof LayerType)[keyof typeof LayerType];
```

From src/Core/Foundations/Domain/Contracts/domain-error.contract.ts (analog pattern):
```typescript
import type { ErrorBase } from "@/Core/Kernel/Contracts/Base/error-base.contract";
export interface DomainErrorContract extends ErrorBase {
  readonly businessRule: string;
  readonly aggregateId: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear scaffold stubs and empty subdirectories</name>

  <files>
    Delete:
    - src/Core/Foundations/Application/Results/application-result.contract.ts
    - src/Core/Foundations/Application/Results/application-result.validator.ts
    - src/Core/Foundations/Application/Results/application-result.type-guard.ts
    - src/Core/Foundations/Application/Errors/Specific/AuthorizationErrors/ (directory + authorization-failed.error.ts inside)
    - src/Core/Foundations/Application/Errors/Specific/ComplaintErrors/ (directory + create-complaint-failed.error.ts inside)
    - src/Core/Foundations/Application/Errors/Specific/AuthenticationErrors/ (empty directory)
    - src/Core/Foundations/Application/Errors/Specific/UserErrors/ (empty directory)
  </files>

  <read_first>
    - src/Core/Foundations/Application/Results/application-result.contract.ts (empty stub)
    - src/Core/Foundations/Application/Results/application-result.validator.ts (empty stub)
    - src/Core/Foundations/Application/Results/application-result.type-guard.ts (empty stub)
    - src/Core/Foundations/Application/Errors/Specific/AuthorizationErrors/authorization-failed.error.ts (empty stub)
    - src/Core/Foundations/Application/Errors/Specific/ComplaintErrors/create-complaint-failed.error.ts (empty stub)
    - src/Core/Foundations/Application/Errors/Specific/AuthenticationErrors/ (empty dir)
    - src/Core/Foundations/Application/Errors/Specific/UserErrors/ (empty dir)
    - .planning/phases/04-application-layer/04-CONTEXT.md § D-12, D-27, D-31–D-33
  </read_first>

  <action>
    Use Remove-Item to delete the three Results stub files (application-result.contract.ts, application-result.validator.ts, application-result.type-guard.ts) and all four Specific subdirectories with -Recurse -Force. These exist only as scaffolding artifacts; D-12 rules that ApplicationResult is a pure type alias needing no contract/validator/type-guard, and D-27 mandates flat file organization for Specific errors (no subdirectories). After deletion, verify the directory tree matches the D-31 canonical structure: Results/ contains only index.ts and application-result.ts; Errors/Specific/ still has its index.ts but no subdirectories.
  </action>

  <verify>
    <automated>Test-Path "src/Core/Foundations/Application/Results/application-result.contract.ts" | Out-Host; if ($?) { exit 1 }; Test-Path "src/Core/Foundations/Application/Errors/Specific/AuthorizationErrors" | Out-Host; if ($?) { exit 1 }; Get-ChildItem "src/Core/Foundations/Application/Errors/Specific" -Directory | Out-Host; $count = (Get-ChildItem "src/Core/Foundations/Application/Errors/Specific" -Directory).Count; if ($count -ne 0) { "SUBDIRECTORIES REMAINING: $count" | Out-Host; exit 1 } else { "OK: No subdirectories remain" | Out-Host }</automated>
    <automated>Remove-Item stubs not present: each target path must not exist after operation</automated>
  </verify>

  <acceptance_criteria>
    - `Test-Path "src/Core/Foundations/Application/Results/application-result.contract.ts"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Results/application-result.validator.ts"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Results/application-result.type-guard.ts"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Errors/Specific/AuthorizationErrors"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Errors/Specific/ComplaintErrors"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Errors/Specific/AuthenticationErrors"` returns `False`
    - `Test-Path "src/Core/Foundations/Application/Errors/Specific/UserErrors"` returns `False`
    - `Errors/Specific/` contains zero subdirectories (only `.ts` files)
  </acceptance_criteria>

  <done>Stub files and empty subdirectories removed; directory tree matches D-31 canonical structure</done>
</task>

<task type="auto">
  <name>Task 2: Create ApplicationErrorContract and RequestContext interfaces</name>

  <files>
    src/Core/Foundations/Application/Contracts/application-error.contract.ts
    src/Core/Foundations/Application/Contracts/request-context.contract.ts
  </files>

  <read_first>
    - .planning/phases/04-application-layer/04-PATTERNS.md § "Contracts/application-error.contract.ts" and "Contracts/request-context.contract.ts" (full code excerpts with exact patterns)
    - .planning/phases/04-application-layer/04-CONTEXT.md D-10 (keep contract), D-16 (RequestContext shape)
    - src/Core/Foundations/Domain/Contracts/domain-error.contract.ts (analog pattern — contract interface extending ErrorBase with readonly properties)
    - src/Core/Kernel/Contracts/Base/error-base.contract.ts (parent contract being extended)
    - src/Core/Kernel/Primitives/Types/correlation-id.type.ts (CorrelationId type + isCorrelationId guard that ApplicationErrorValidator will use)
  </read_first>

  <action>
    Write `Contracts/application-error.contract.ts`:
    Import `ErrorBase` type from `@/Core/Kernel/Contracts/Base/error-base.contract` and `CorrelationId` type from `@/Core/Kernel/Primitives/Types/correlation-id.type`.
    Export `ApplicationErrorContract` interface extending `ErrorBase` with three readonly fields:
    - `operationName: string` — per D-03, represents the UseCase name
    - `correlationId: CorrelationId` — per D-04, required for distributed tracing
    - `userId?: string` — per D-05, optional for background jobs/system tasks

    Write `Contracts/request-context.contract.ts`:
    Import `CorrelationId` type from `@/Core/Kernel/Primitives/Types/correlation-id.type`.
    Export `RequestContext` interface with two readonly fields:
    - `correlationId: CorrelationId` — required, per D-16
    - `userId?: string` — optional, per D-16

    Both files must use `export type` for interfaces (consistent with Domain pattern). Leaf import paths must use `@/` alias convention consistent with the existing codebase.
  </action>

  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1</automated>
    <automated>Select-String -Path "src/Core/Foundations/Application/Contracts/application-error.contract.ts" -Pattern "extends ErrorBase" | Out-Host; if (-not $?) { "MISSING: extends ErrorBase" | Out-Host; exit 1 }</automated>
    <automated>Select-String -Path "src/Core/Foundations/Application/Contracts/application-error.contract.ts" -Pattern "operationName|correlationId|userId" | Out-Host; if (-not $?) { "MISSING: contract fields" | Out-Host; exit 1 }</automated>
    <automated>Select-String -Path "src/Core/Foundations/Application/Contracts/request-context.contract.ts" -Pattern "correlationId" | Out-Host; if (-not $?) { "MISSING: correlationId" | Out-Host; exit 1 }</automated>
  </verify>

  <acceptance_criteria>
    - `application-error.contract.ts` contains `export interface ApplicationErrorContract extends ErrorBase` with `readonly operationName: string`, `readonly correlationId: CorrelationId`, and `readonly userId?: string`
    - `request-context.contract.ts` contains `export interface RequestContext` with `readonly correlationId: CorrelationId` and `readonly userId?: string`
    - Both imports use `@/Core/Kernel/` aliases consistent with existing codebase convention
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>

  <done>ApplicationErrorContract and RequestContext interfaces exist, TypeScript compiles clean, contracts match D-03/D-04/D-05/D-10/D-16</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract interface → consumer | Typed interface consumed by ApplicationError class and downstream layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | ApplicationErrorContract | mitigate | No sensitive data in interface properties; `userId` is optional and typed as `string` (not PII-labeled); correlationId is a branded opaque string, not PII |
| T-04-02 | Information Disclosure | RequestContext | mitigate | `userId` field is optional and unlabeled as PII; `correlationId` is opaque — no embedding of request details |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- All 3 Results stub files are absent
- All 4 Specific subdirectories are absent
- Both contract files have correct exports and imports
</verification>

<success_criteria>
1. ApplicationErrorContract interface extends ErrorBase with operationName, correlationId, userId — matches D-10 exact spec
2. RequestContext interface has correlationId (required) and userId (optional) — matches D-16
3. Zero scaffold stubs remain — D-12 cleanup complete
4. Zero empty subdirectories in Specific/ — D-27 flat structure prepared
5. TypeScript compilation passes with no errors
</success_criteria>

<output>
Create `.planning/phases/04-application-layer/04-01-SUMMARY.md` when done
</output>
