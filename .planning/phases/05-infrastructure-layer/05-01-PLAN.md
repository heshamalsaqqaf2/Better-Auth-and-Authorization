---
phase: 05-infrastructure-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Core/Foundations/Infrastructure/Contracts/infrastructure-component.type.ts
  - src/Core/Foundations/Infrastructure/Contracts/infrastructure-retry-strategy.type.ts
  - src/Core/Foundations/Infrastructure/Contracts/infrastructure-error.contract.ts
  - src/Core/Foundations/Infrastructure/Contracts/index.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/database-connection.error.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/database-query.error.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/index.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/api-timeout.error.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/api-unavailable.error.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/index.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/cache-unavailable.error.ts
  - src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/index.ts
  - src/Core/Foundations/Infrastructure/Results/infrastructure-result.contract.ts
  - src/Core/Foundations/Infrastructure/Results/infrastructure-result.validator.ts
  - src/Core/Foundations/Infrastructure/Results/infrastructure-result.type-guard.ts
  - src/Core/Foundations/Infrastructure/Mappers/application-to-infrastructure-error.mapper.ts
autonomous: true
requirements: [INFO-01]
user_setup: []

must_haves:
  truths:
    - "InfrastructureErrorContract interface is defined with systemComponent, retryCount?, retryStrategy?, safeDetails?"
    - "InfrastructureComponent union type exists: 'Database' | 'Network' | 'Cache'"
    - "InfrastructureRetryStrategy union type exists: 'exponential' | 'fixed' | 'jitter'"
    - "Contracts/index.ts barrel re-exports all three contract types"
    - "Scaffold stubs that contradict pure type alias decision are removed"
    - "Old application-to-infrastructure-error.mapper.ts stub is removed"
    - "Category subdirectories under Errors/Specific/ are removed"
  artifacts:
    - path: "src/Core/Foundations/Infrastructure/Contracts/infrastructure-component.type.ts"
      provides: "InfrastructureComponent union type"
      min_lines: 3
      contains: "type InfrastructureComponent"
    - path: "src/Core/Foundations/Infrastructure/Contracts/infrastructure-retry-strategy.type.ts"
      provides: "InfrastructureRetryStrategy union type"
      min_lines: 3
      contains: "type InfrastructureRetryStrategy"
    - path: "src/Core/Foundations/Infrastructure/Contracts/infrastructure-error.contract.ts"
      provides: "InfrastructureErrorContract interface extending ErrorBase"
      min_lines: 10
      contains: "interface InfrastructureErrorContract"
    - path: "src/Core/Foundations/Infrastructure/Contracts/index.ts"
      provides: "Contracts barrel"
      min_lines: 3
  key_links:
    - from: "infrastructure-error.contract.ts"
      to: "Core/Kernel/Contracts/Base/error-base.contract.ts"
      via: "import type { ErrorBase } from @/Core/Kernel/..."
      pattern: "import.*ErrorBase.*error-base"
    - from: "infrastructure-component.type.ts"
      to: "infrastructure-error.contract.ts"
      via: "used as systemComponent property type"
    - from: "infrastructure-retry-strategy.type.ts"
      to: "infrastructure-error.contract.ts"
      via: "used as retryStrategy property type"
---

<objective>
Establish Infrastructure layer contracts and remove scaffold stubs that contradict architectural decisions.

**Purpose:** Define the typed vocabulary for Infrastructure errors (component types, retry strategies, error contract interface) before implementing the error class. Clean up scaffold stubs that are explicitly undesired per D-15 (InfrastructureResult contract/validator/type-guard), D-21 (old mapper direction), and D-23 (category subdirectories flattening).

**Output:**
- `Contracts/infrastructure-component.type.ts` — `'Database' | 'Network' | 'Cache'`
- `Contracts/infrastructure-retry-strategy.type.ts` — `'exponential' | 'fixed' | 'jitter'`
- `Contracts/infrastructure-error.contract.ts` — `InfrastructureErrorContract` extending `ErrorBase`
- `Contracts/index.ts` — re-export barrel
- Deleted: 3 Results stubs, 1 old mapper stub, 3 category subdirectories
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-infrastructure-layer/05-CONTEXT.md

<interfaces>
<!-- Key types the executor needs to reference. No codebase exploration needed. -->

From `src/Core/Kernel/Contracts/Base/error-base.contract.ts`:
```typescript
export interface ErrorBase {
  readonly code: string;
  readonly message: string;
  readonly timestamp: Date;
  readonly layer: LayerType;
  readonly cause?: ErrorBase;
  isRecoverable(): boolean;
  getSeverity(): Severity;
  toJSON(): Record<string, unknown>;
}
```

From `src/Core/Kernel/Primitives/Enums/layer-type.enum.ts`:
```typescript
export const LayerType = {
  DOMAIN: 'domain', APPLICATION: 'application',
  INFRASTRUCTURE: 'infrastructure', PRESENTATION: 'presentation',
} as const;
export type LayerType = (typeof LayerType)[keyof typeof LayerType];
```

From `src/Core/Foundations/Domain/Contracts/domain-error.contract.ts` (reference pattern):
```typescript
import type { ErrorBase } from "@/Core/Kernel/Contracts/Base/error-base.contract";
export interface DomainErrorContract extends ErrorBase {
  readonly businessRule: string;
  readonly aggregateId: string;
}
```
</interfaces>

Existing scaffold files to read before deletion:
- `src/Core/Foundations/Infrastructure/Results/infrastructure-result.contract.ts` — empty stub, to DELETE
- `src/Core/Foundations/Infrastructure/Results/infrastructure-result.validator.ts` — empty stub, to DELETE
- `src/Core/Foundations/Infrastructure/Results/infrastructure-result.type-guard.ts` — empty stub, to DELETE
- `src/Core/Foundations/Infrastructure/Mappers/application-to-infrastructure-error.mapper.ts` — empty stub, to DELETE

Existing scaffold directories to delete:
- `src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/` — contains empty stubs, to DELETE (per D-23)
- `src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/` — contains empty stubs, to DELETE (per D-23)
- `src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/` — contains empty stubs, to DELETE (per D-23)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete scaffold stubs that contradict architectural decisions</name>
  <files>
    <file>src/Core/Foundations/Infrastructure/Results/infrastructure-result.contract.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Results/infrastructure-result.validator.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Results/infrastructure-result.type-guard.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Mappers/application-to-infrastructure-error.mapper.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/database-connection.error.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/database-query.error.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/index.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/DatabaseErrors/ — DELETE dir</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/api-timeout.error.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/api-unavailable.error.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/index.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/NetworkErrors/ — DELETE dir</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/cache-unavailable.error.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/index.ts — DELETE</file>
    <file>src/Core/Foundations/Infrastructure/Errors/Specific/CacheErrors/ — DELETE dir</file>
  </files>
  <action>
    Delete the following scaffold files and directories using the filesystem. These are empty stubs that were created during the initial scaffold but are now explicitly contradicted by architectural decisions:
    
    Per D-15: `Results/infrastructure-result.contract.ts`, `infrastructure-result.validator.ts`, `infrastructure-result.type-guard.ts` — InfrastructureResult is a pure type alias (D-14), these contract/validator/type-guard stubs are unwanted.
    
    Per D-21: `Mappers/application-to-infrastructure-error.mapper.ts` — the mapper direction has been renamed to Infrastructure→Application (D-21). This stub will be replaced by `infrastructure-to-application-error.mapper.ts` in a later plan.
    
    Per D-23: `Errors/Specific/DatabaseErrors/`, `Errors/Specific/NetworkErrors/`, `Errors/Specific/CacheErrors/` — category subdirectories are being flattened. All 5 specific error files inside them are empty stubs; real implementations will be created directly under `Errors/Specific/` in a later plan. Delete the individual files first, then delete the parent subdirectories in this order: DatabaseErrors, NetworkErrors, CacheErrors. The `Errors/Specific/index.ts` and `Errors/Specific/` parent directory must survive — only the category subdirectories are removed.
    
    Avoid using `Remove-Item -Recurse` on any directory that might contain non-target files. Check each directory's contents before deletion.
  </action>
  <verify>
    <automated>
      # Verify each deleted file is gone
      $deleted = @(
        "Results/infrastructure-result.contract.ts",
        "Results/infrastructure-result.validator.ts",
        "Results/infrastructure-result.type-guard.ts",
        "Mappers/application-to-infrastructure-error.mapper.ts",
        "Errors/Specific/DatabaseErrors/database-connection.error.ts",
        "Errors/Specific/DatabaseErrors/database-query.error.ts",
        "Errors/Specific/DatabaseErrors/index.ts",
        "Errors/Specific/NetworkErrors/api-timeout.error.ts",
        "Errors/Specific/NetworkErrors/api-unavailable.error.ts",
        "Errors/Specific/NetworkErrors/index.ts",
        "Errors/Specific/CacheErrors/cache-unavailable.error.ts",
        "Errors/Specific/CacheErrors/index.ts"
      )
      $missing = $true
      $root = "src/Core/Foundations/Infrastructure"
      foreach ($f in $deleted) {
        if (Test-Path -LiteralPath "$root/$f") { $missing = $false; Write-Host "STILL EXISTS: $f" }
      }
      # Verify category directories are gone but parent Specific/ exists
      if (Test-Path -LiteralPath "$root/Errors/Specific/DatabaseErrors") { $missing = $false; Write-Host "STILL EXISTS: DatabaseErrors/" }
      if (Test-Path -LiteralPath "$root/Errors/Specific/NetworkErrors") { $missing = $false; Write-Host "STILL EXISTS: NetworkErrors/" }
      if (Test-Path -LiteralPath "$root/Errors/Specific/CacheErrors") { $missing = $false; Write-Host "STILL EXISTS: CacheErrors/" }
      if (!(Test-Path -LiteralPath "$root/Errors/Specific")) { $missing = $false; Write-Host "ERROR: Errors/Specific/ was deleted but should survive" }
      if (!$missing) { throw "Some files still exist" }
      Write-Host "OK: All scaffold stubs deleted"
    </automated>
  </verify>
  <done>
    All 3 Results stubs deleted, old Mappers stub deleted, 3 category subdirectories under Errors/Specific/ removed. Errors/Specific/ parent directory survives empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Contracts types, contract interface, and barrel</name>
  <files>
    <file>src/Core/Foundations/Infrastructure/Contracts/infrastructure-component.type.ts — CREATE</file>
    <file>src/Core/Foundations/Infrastructure/Contracts/infrastructure-retry-strategy.type.ts — CREATE</file>
    <file>src/Core/Foundations/Infrastructure/Contracts/infrastructure-error.contract.ts — MODIFY (was empty stub)</file>
    <file>src/Core/Foundations/Infrastructure/Contracts/index.ts — MODIFY (was empty stub)</file>
  </files>
  <action>
    Create four files in the Contracts/ subdirectory following the Domain contract pattern (04-PATTERNS.md §Contract Interface Pattern):
    
    1. **`Contracts/infrastructure-component.type.ts`** — Export a type alias (not an interface) per D-03:
       ```typescript
       export type InfrastructureComponent = 'Database' | 'Network' | 'Cache';
       ```
       This is a controlled string union — not `as const` + type extraction pattern — because it is a simple controlled vocabulary with only 3 values.
    
    2. **`Contracts/infrastructure-retry-strategy.type.ts`** — Export a type alias per D-04:
       ```typescript
       export type InfrastructureRetryStrategy = 'exponential' | 'fixed' | 'jitter';
       ```
    
    3. **`Contracts/infrastructure-error.contract.ts`** — Modify the empty stub to define `InfrastructureErrorContract` per D-10. Follow the DomainErrorContract pattern from `src/Core/Foundations/Domain/Contracts/domain-error.contract.ts`. Import `ErrorBase` from `@/Core/Kernel/Contracts/Base/error-base.contract`. The interface extends `ErrorBase` and adds:
       - `readonly systemComponent: InfrastructureComponent` (D-03)
       - `readonly retryCount?: number` (D-02)
       - `readonly retryStrategy?: InfrastructureRetryStrategy` (D-04)
       - `readonly safeDetails?: Record<string, unknown>` (D-05)
       
       Import `InfrastructureComponent` from `./infrastructure-component.type` and `InfrastructureRetryStrategy` from `./infrastructure-retry-strategy.type`. All four new properties are `readonly`. Do NOT add a `technicalCause` field (D-01 — reuse inherited `cause`). Do NOT add `operationName` or `correlationId` — those are Application-layer concepts.
    
    4. **`Contracts/index.ts`** — Modify the empty stub to create a barrel that re-exports:
       - `export type { InfrastructureErrorContract } from "./infrastructure-error.contract";`
       - `export type { InfrastructureComponent } from "./infrastructure-component.type";`
       - `export type { InfrastructureRetryStrategy } from "./infrastructure-retry-strategy.type";`
       
       Follow the naming convention from Phase 4 PATTERNS.md barrier pattern: type-only exports use `export type { ... }`.
  </action>
  <verify>
    <automated>
      npm run typecheck 2>&1; if ($LASTEXITCODE -ne 0) { throw "TypeScript compilation failed" }
      Write-Host "OK: Contracts compile clean"
    </automated>
  </verify>
  <done>
    InfrastructureComponent, InfrastructureRetryStrategy, InfrastructureErrorContract defined and exported from Contracts/index.ts. TypeScript compilation passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Infrastructure→Contracts | Type definitions define the shape of error data that may cross layer boundaries — contract interfaces are the safety contract for information disclosure prevention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | InfrastructureErrorContract.safeDetails | mitigate | Contracts define safeDetails as the ONLY field allowed to cross the Application boundary (D-07). Raw error fields (message, cause) are blocked at the mapper layer — the contract enforces the shape but the sanitizer does the stripping. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero external dependencies for Foundation layers — no package installs in this plan |
</threat_model>

<verification>
- [ ] All 12 deleted files confirmed absent from filesystem
- [ ] All 3 deleted category directories confirmed absent
- [ ] Contracts directory has exactly 4 files: 3 type/interface definitions + 1 barrel
- [ ] `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>
1. InfrastructureErrorContract interface exists with systemComponent, retryCount?, retryStrategy?, safeDetails? properties
2. InfrastructureComponent = 'Database' | 'Network' | 'Cache'
3. InfrastructureRetryStrategy = 'exponential' | 'fixed' | 'jitter'
4. All three types re-exported from Contracts/index.ts
5. Results stubs deleted (D-15 compliance)
6. Old application-to-infrastructure-error.mapper.ts deleted (D-21 compliance)
7. Category subdirectories under Errors/Specific/ deleted (D-23 compliance)
8. TypeScript compilation clean
</success_criteria>

<output>
Create `.planning/phases/05-infrastructure-layer/05-01-SUMMARY.md` when done
</output>
