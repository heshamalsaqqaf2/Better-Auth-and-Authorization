---
phase: 08-module-integration-server-actions
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - proxy.ts
  - src/Lib/RequestContext/store.ts
  - src/Lib/RequestContext/index.ts
autonomous: true
requirements: [MIDDLEWARE, CORRELATION, PROXY]
must_haves:
  truths:
    - "proxy.ts propagates x-correlation-id via request/response headers on every request"
    - "Request context store provides getter/setter for correlation ID"
    - "Middleware runs on every request with minimal overhead"
    - "Server Actions initialize AsyncLocalStorage via withRequestContext() at the action boundary"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Next.js 16 middleware that propagates x-correlation-id header"
    - path: "src/Lib/RequestContext/store.ts"
      provides: "AsyncLocalStorage instance and typed store shape"
      contains: "AsyncLocalStorage"
    - path: "src/Lib/RequestContext/init.ts"
      provides: "withRequestContext() helper for Server Actions to initialize ALS"
      contains: "runWithStore"
  key_links:
    - from: "proxy.ts"
      to: "src/Lib/RequestContext"
      pattern: "x-correlation-id header"
    - from: "src/Lib/RequestContext/init.ts"
      to: "src/Lib/RequestContext/store.ts"
      pattern: "runWithStore"
---

<objective>
Create the correlation ID propagation middleware (proxy.ts) and AsyncLocalStorage request context store. Middleware propagates correlation ID via headers only (ALS cannot persist across the Next.js middleware boundary). Server Actions initialize ALS via withRequestContext() at the action entry point.

Purpose: Every request gets a unique correlation ID that flows through middleware → Server Action → UseCase → Repository → DB. Enables end-to-end tracing without manual ID threading.
</objective>

<execution_context>
@.planning/phases/08-module-integration-server-actions/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@node_modules/next/dist/docs/ (Next.js 16 proxy.ts conventions)
</execution_context>

<context>
@proxy.ts (DOES NOT EXIST — create at project root)
@src/Core/Kernel/Primitives/Types/correlation-id.type.ts (CorrelationId branded type)
@src/Lib/ (directory exists, subdirectory RequestContext to be created)

<interfaces>

**proxy.ts contract (Next.js 16 convention per AGENTS.md):**
- File at `src/proxy.ts`
- Exports default async function `proxy(request: NextRequest): Promise<NextResponse | undefined>`
- Generates a correlation ID via `crypto.randomUUID()` per request
- Sets `x-correlation-id` header on both request (for downstream propagation) and response
- Does NOT initialize AsyncLocalStorage — middleware scope exits before page handler runs
- Calls `NextResponse.next()` to continue the request pipeline

**RequestContext store contract:**
- Typed store shape: `{ correlationId: string; startTime: number }`
- `getCorrelationId(): string | undefined` — read from async context
- `setCorrelationId(id: string): void` — write to async context
- `getStore(): Store | undefined` — raw store access
- Export `requestContextStore` AsyncLocalStorage instance

**CorrelationId integration:**
- Generate correlation ID using crypto.randomUUID()
- Wrap the ID in the Kernel's CorrelationId branded type if the factory is importable
- If not importable (layer boundary), use a plain string with same format

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create RequestContext store</name>
  <files>src/Lib/RequestContext/store.ts</files>
  <read_first>
    - src/Core/Kernel/Primitives/Types/correlation-id.type.ts (CorrelationId branded type and factory)
    - AGENTS.md (Next.js 16 proxy conventions)
  </read_first>
  <action>
    In `src/Lib/RequestContext/store.ts`:
    - Import `{ AsyncLocalStorage }` from `node:async_hooks`
    - Define `RequestContextStore` interface: `{ correlationId: string; startTime: number }`
    - Create and export `requestContextStore = new AsyncLocalStorage<RequestContextStore>()`
    - Create `setCorrelationId(id: string): void` — wraps `requestContextStore.getStore()` and assigns
    - Create `getCorrelationId(): string | undefined` — reads from store
    - Create `getStore(): RequestContextStore | undefined` — raw store access
    - Create `runWithStore<T>(store: RequestContextStore, fn: () => Promise<T>): Promise<T>` — convenience wrapper around requestContextStore.run()
  </action>
  <acceptance_criteria>
    - `src/Lib/RequestContext/store.ts` exports AsyncLocalStorage instance and helper functions
    - Store type has `correlationId: string` and `startTime: number`
    - Helper functions correctly read/write to the async context
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create RequestContext barrel</name>
  <files>src/Lib/RequestContext/index.ts</files>
  <read_first>
    - src/Lib/RequestContext/store.ts
  </read_first>
  <action>
    In `src/Lib/RequestContext/index.ts`:
    - Re-export all exports from `./store`
  </action>
  <acceptance_criteria>
    - Barrel re-exports store, getCorrelationId, setCorrelationId, getStore, runWithStore, RequestContextStore
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create proxy.ts middleware (header propagation only)</name>
  <files>src/proxy.ts</files>
  <read_first>
    - AGENTS.md (Next.js 16 proxy conventions)
    - node_modules/next/dist/docs/ (proxy.ts API documentation)
  </read_first>
  <action>
    In `src/proxy.ts`:
    - Import `{ NextRequest, NextResponse }` from `next/server`
    - Export default async function `proxy(request: NextRequest): Promise<NextResponse | undefined>`
    - In the function:
      - Generate correlation ID via `crypto.randomUUID()`
      - Clone request headers and set `x-correlation-id`
      - Call `NextResponse.next()` with the modified request headers
      - Set `x-correlation-id` on the response headers
      - Return the response
    - Do NOT import or use AsyncLocalStorage/runWithStore — ALS scope cannot persist across Next.js middleware boundary
    - Keep it minimal per D-07: no auth checks, no logging, no redirects — single responsibility: correlation ID propagation
  </action>
  <acceptance_criteria>
    - `src/proxy.ts` exists with default async export
    - Function signature matches Next.js 16 proxy convention
    - `x-correlation-id` header set on both request forwarding and response
    - No AsyncLocalStorage or runWithStore import
    - No auth guard logic (per D-07)
    - TypeScript compiles clean with `npx tsc --noEmit`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Network → proxy.ts | All requests pass through proxy.ts — no auth or validation at this layer |

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-03 | Spoofing | proxy.ts | accept | Correlation ID is for tracing, not auth; spoofed ID has no security impact |
</threat_model>

<verification>
- `npx tsc --noEmit` succeeds
- `src/proxy.ts` is valid Next.js 16 middleware (doesn't use deprecated APIs)
- `x-correlation-id` header flows from middleware → request forwarding → response
- AsyncLocalStorage store is kept for Server Actions to initialize via withRequestContext()
</verification>

<success_criteria>
1. proxy.ts at src/proxy.ts propagates x-correlation-id via headers on every request
2. RequestContext store provides get/set helper functions (ready for ALS init in Server Actions)
3. Response headers include x-correlation-id
4. withRequestContext() helper available for Server Actions to initialize ALS
5. All files type-check clean
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/08-module-integration-server-actions/08-02-SUMMARY.md`
</output>
