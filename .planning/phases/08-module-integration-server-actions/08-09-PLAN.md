---
phase: 08-module-integration-server-actions
plan: 09
type: execute
wave: 3
depends_on: [07]
files_modified:
  - src/app/(user)/dashboard/page.tsx
  - src/app/(user)/dashboard/error.tsx
  - src/app/(user)/dashboard/layout.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(admin)/dashboard/error.tsx
  - src/app/(admin)/dashboard/layout.tsx
  - src/app/error.tsx
  - src/app/layout.tsx
  - src/Modules/Authentication/Presentation/Components/user-nav.tsx
  - src/Modules/Authentication/Presentation/Components/sign-out-button.tsx
  - src/Modules/Authentication/Presentation/Components/index.ts
autonomous: true
requirements: [UI-DASHBOARD, UI-ERROR-BOUNDARY, UI-NAV]
must_haves:
  truths:
    - "User dashboard shows basic welcome message and sign-out button"
    - "Admin dashboard shows basic admin message and sign-out button (blank — no RBAC in Phase 8)"
    - "Both dashboards have per-route error boundaries with contextual recovery options"
    - "Root error boundary catches unhandled errors across all routes"
    - "Error boundaries log full cause-chain context for debugging"
    - "Sign-out button calls signOutAction Server Action"
  artifacts:
    - path: "src/app/(user)/dashboard/page.tsx"
      provides: "User dashboard — blank/initialization only"
    - path: "src/app/(user)/dashboard/error.tsx"
      provides: "Per-route error boundary with user-contextual recovery"
    - path: "src/app/(admin)/dashboard/page.tsx"
      provides: "Admin dashboard — blank/initialization only"
    - path: "src/app/error.tsx"
      provides: "Global error boundary with generic recovery UI"
    - path: "src/Modules/Authentication/Presentation/Components/sign-out-button.tsx"
      provides: "Sign-out button that calls signOutAction"
  key_links:
    - from: "error-boundaries"
      to: "src/Core/Foundations/Presentation/Errors/"
      pattern: "PresentationError"
---

<objective>
Create the user and admin dashboard pages, sign-out button component, and global + per-route error boundaries.

Purpose: Dashboards are the authenticated landing pages. User dashboard is basic welcome + sign-out. Admin dashboard is basic admin message. Both have route-level error boundaries. The global error boundary catches anything that falls through. Sign-out button provides a consistent way to end sessions.
</objective>

<execution_context>
@.planning/phases/08-module-integration-server-actions/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@src/Modules/Authentication/Presentation/Actions/sign-out.action.ts (signOutAction — created in Plan 07)
@src/Modules/Authentication/Presentation/Components/ (components barrel)
@src/Shared/components/ui/ (shadcn/ui components: Card, Button, Avatar)
@src/app/layout.tsx (root layout — add error boundary awareness)

<interfaces>

**Dashboard page structure (D-10):**
- Pages are blank/initialization only
- Display: welcome message, user info, sign-out button
- Admin dashboard additionally shows "Admin Panel" heading (no actual admin features)
- Both dashboards render inside their group-specific layout

**Error boundary contract (D-12):**
```typescript
// error.tsx (Next.js 16 — must be Client Component)
'use client';

export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
  // Log error with full cause-chain context
  // Show retry button
  // Show contextual recovery options
}
```

**Error boundary levels (D-12):**
1. Root `src/app/error.tsx` — generic recovery UI, retry button
2. `(user)/dashboard/error.tsx` — user-contextual: "Go to sign-in" link + retry
3. `(admin)/dashboard/error.tsx` — admin-contextual: retry + "go to user dashboard"

**Sign-out button contract:**
```typescript
// Client Component
export function SignOutButton() {
  // Calls signOutAction on click
  // Shows loading state during sign-out
  // Redirects to /sign-in after success
}

// user-nav.tsx — Higher-level component
// Shows user name, avatar, sign-out button
```

**Dashboard layout:**
- Both dashboards have a simple layout with navigation header
- Header shows sign-out button and basic branding
- No sidebar or complex navigation in Phase 8
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create sign-out button component</name>
  <files>src/Modules/Authentication/Presentation/Components/sign-out-button.tsx</files>
  <read_first>
    - src/Modules/Authentication/Presentation/Actions/sign-out.action.ts (signOutAction)
    - src/Core/Foundations/Presentation/Results/presentation-result.type.ts (PresentationResult)
    - src/Shared/components/ui/button.tsx (Button component)
  </read_first>
  <action>
    In `src/Modules/Authentication/Presentation/Components/sign-out-button.tsx`:
    - `'use client'`
    - Import `useActionState` from `react`
    - Import `signOutAction` from Actions
    - Import `Button` from shadcn/ui
    - Import `useRouter` from `next/navigation`
    - Import `toast` from `sonner`
    - Create initial state: `const initialState: PresentationResult<null> = successResult(null)`
    - Export default function `SignOutButton()`:
      - `const [state, formAction, isPending] = useActionState(signOutAction, initialState)`
      - `const router = useRouter()`
      - `useEffect`: if state._tag === 'SUCCESS', router.push('/sign-in'); if state._tag === 'FAILURE', toast.error(...)
      - Render `<form action={formAction}>` with a Button of variant "outline" or "destructive"
      - Button text: "Sign Out" (or loading spinner when isPending)
  </action>
  <acceptance_criteria>
    - Client Component with useActionState for sign-out
    - Renders form with signOutAction
    - Loading state during pending
    - SUCCESS redirects to /sign-in
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create user navigation component</name>
  <files>src/Modules/Authentication/Presentation/Components/user-nav.tsx</files>
  <read_first>
    - src/Modules/Authentication/Presentation/Components/sign-out-button.tsx
    - src/Shared/components/ui/ (Button, Avatar if available)
  </read_first>
  <action>
    In `src/Modules/Authentication/Presentation/Components/user-nav.tsx`:
    - `'use client'`
    - Export default function `UserNav({ user }: { user: { name: string; email: string } })`:
      - Display user name and email (basic header text)
      - Render SignOutButton
      - Keep it minimal — just a flex row header with user info and sign-out
  </action>
  <acceptance_criteria>
    - Component shows user info and sign-out button
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create user dashboard page and layout</name>
  <files>
    src/app/(user)/dashboard/page.tsx
    src/app/(user)/dashboard/layout.tsx
  </files>
  <read_first>
    - src/Modules/Authentication/Presentation/Components/user-nav.tsx
  </read_first>
  <action>
    In `src/app/(user)/dashboard/layout.tsx`:
    - Server Component layout
    - Fetch current user session using the session utility
    - Import UserNav component
    - Render: header bar with UserNav, then children
    - If no session, redirect to /sign-in (basic auth check)

    In `src/app/(user)/dashboard/page.tsx`:
    - Server Component
    - Fetch session to get current user
    - Render: welcome message ("Welcome, {user.name}!"), basic card with user email
    - Blank/initialization only per D-10
  </action>
  <acceptance_criteria>
    - Layout renders UserNav header
    - Dashboard shows welcome with user name
    - Session check redirects to /sign-in if not authenticated
    - Pages are blank/initialization only (D-10)
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create admin dashboard page and layout</name>
  <files>
    src/app/(admin)/dashboard/page.tsx
    src/app/(admin)/dashboard/layout.tsx
  </files>
  <read_first>
    - src/app/(user)/dashboard/layout.tsx (pattern reference)
  </read_first>
  <action>
    In `src/app/(admin)/dashboard/layout.tsx`:
    - Server Component layout
    - Same pattern as user dashboard layout
    - Fetch session, redirect if not authenticated
    - Render UserNav header

    In `src/app/(admin)/dashboard/page.tsx`:
    - Server Component
    - Render: "Admin Panel" heading, welcome message
    - Blank/initialization only per D-10
    - No RBAC in Phase 8 — admin dashboard is accessible to any authenticated user
  </action>
  <acceptance_criteria>
    - Admin layout with UserNav header
    - Admin dashboard page with "Admin Panel" heading
    - Empty/initialization content (D-10)
    - No RBAC gating in Phase 8
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create per-route error boundaries</name>
  <files>
    src/app/(admin)/dashboard/error.tsx
    src/app/(user)/dashboard/error.tsx
  </files>
  <read_first>
    - Node.js docs / next docs for Next.js 16 error boundary conventions
    - src/app/layout.tsx (existing root layout)
  </read_first>
  <action>
    Create two route-specific error boundaries:

    In `src/app/(user)/dashboard/error.tsx`:
    - `'use client'`
    - Export default `Error({ error, reset })`:
      - `useEffect` to log error details: `console.error('User dashboard error:', error)`
      - Log the full error chain including `error.cause` if available
      - Render Card with: "Something went wrong" heading, user-friendly message, retry button (calls reset()), "Go to sign in" link to /sign-in
    - Use shadcn/ui Card component

    In `src/app/(admin)/dashboard/error.tsx`:
    - `'use client'`
    - Same pattern as user dashboard error
    - Options: retry button, "Go to user dashboard" link (/(user)/dashboard)
    - Log full cause-chain context
  </action>
  <acceptance_criteria>
    - Both error boundaries are Client Components
    - Log full cause-chain context (error, error.cause, error.digest)
    - User dashboard error includes: retry + sign-in link
    - Admin dashboard error includes: retry + user dashboard link
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create global error boundary</name>
  <files>src/app/error.tsx</files>
  <read_first>
    - Existing src/app/layout.tsx (root layout to understand wrapper structure)
  </read_first>
  <action>
    In `src/app/error.tsx`:
    - `'use client'`
    - Export default `Error({ error, reset })`:
      - `useEffect` to log error with full cause-chain: `console.error('Unhandled error:', { message: error.message, cause: error.cause, digest: error.digest })`
      - Render generic recovery UI: "Something went wrong" heading, "Please try again" message, retry button
      - Use shadcn/ui Card and Button for styling
      - Minimal, clean, non-technical user-facing message
  </action>
  <acceptance_criteria>
    - Client Component
    - Logs full error details including cause chain
    - Shows retry button
    - User-friendly generic message
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Dashboard → Server Action | Sign-out flow triggered from dashboard UI |
| Dashboard → Error boundary | Unhandled errors in dashboard caught by per-route or global boundary |

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-17 | Info Disclosure | error.tsx | mitigate | Error boundaries log full context to server console but show only generic messages to users |
| T-08-18 | Session Hijacking | sign-out | mitigate | Better Auth handles session invalidation server-side; client-side sign-out button is only a trigger |
</threat_model>

<verification>
- `npx tsc --noEmit` succeeds
- Error boundaries compile as Client Components
- Dashboard pages and layouts compile correctly
- Sign-out button form action is properly typed
</verification>

<success_criteria>
1. User dashboard with welcome message and sign-out button
2. Admin dashboard with "Admin Panel" heading (blank/initialization)
3. Sign-out button calls signOutAction with loading state and redirect
4. Per-route error boundaries for both dashboards with contextual recovery
5. Global error boundary at root level with generic recovery UI
6. Error boundaries log full cause-chain context for debugging
7. All files type-check clean
8. Dashboards are blank/initialization only (D-10)
9. No RBAC — admin dashboard accessible to any authenticated user (D-10)
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/08-module-integration-server-actions/08-09-SUMMARY.md`
</output>
