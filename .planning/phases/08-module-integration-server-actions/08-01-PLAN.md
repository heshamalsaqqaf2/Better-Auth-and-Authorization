---
phase: 08-module-integration-server-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Lib/BetterAuth/Config/server.ts
  - src/Lib/BetterAuth/Config/client.ts
  - src/Lib/BetterAuth/Config/index.ts
  - src/Lib/BetterAuth/utils/get-session.ts
  - src/CompositionRoot/container.ts
  - src/CompositionRoot/Bindings/index.ts
  - src/CompositionRoot/Containers/index.ts
  - src/Lib/BetterAuth/schema.ts
  - package.json
autonomous: true
requirements: [BA-CONFIG, BA-CLIENT, BA-UTILS, CR-BINDINGS]
must_haves:
  truths:
    - "Better Auth server config with email/password plugin is exported and valid"
    - "Better Auth client config is wired and exports the client instance"
    - "get-session utility correctly extracts current user session from request headers"
    - "Composition Root container.ts is populated with concrete bindings for all Authentication module dependencies"
  artifacts:
    - path: "src/Lib/BetterAuth/Config/server.ts"
      provides: "Better Auth server instance with email/password plugin"
      contains: "auth() call"
    - path: "src/Lib/BetterAuth/Config/client.ts"
      provides: "Better Auth client instance for browser"
      contains: "createAuthClient()"
    - path: "src/CompositionRoot/container.ts"
      provides: "Dependency injection container with wired authentication dependencies"
      exports: ["container", "getContainer()"]
  key_links:
    - from: "src/Lib/BetterAuth/Config/server.ts"
      to: "src/Lib/BetterAuth/Config/client.ts"
      pattern: "better-auth/client"
    - from: "src/CompositionRoot/Bindings/index.ts"
      to: "src/CompositionRoot/container.ts"
      pattern: "container.bind"
---

<objective>
Wire the Better Auth server and client configuration, create the session utility, and set up the Composition Root with concrete dependency bindings.

Purpose: Establish the auth infrastructure foundation that all Server Actions and use cases depend on. Better Auth configs are the external boundary, Composition Root is the internal wiring hub.
</objective>

<execution_context>
@.planning/phases/08-module-integration-server-actions/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
@src/Lib/BetterAuth/Config/server.ts (EMPTY — needs better-auth server config with email/password)
@src/Lib/BetterAuth/Config/client.ts (EMPTY — needs createAuthClient with base URL)
@src/Lib/BetterAuth/Config/index.ts (EMPTY — needs barrel export)
@src/Lib/BetterAuth/utils/get-session.ts (EMPTY — needs session extraction from headers)
@src/CompositionRoot/container.ts (EMPTY — needs DI container initialization)
@src/CompositionRoot/Bindings/index.ts (EMPTY — needs binding definitions)
@src/CompositionRoot/Containers/index.ts (EMPTY — needs container factory)

<interfaces>

**Better Auth server config contract:**
- Import `better-auth` and `@better-auth/email-password`
- Create `auth()` with `emailAndPassword` plugin
- Use Drizzle adapter with existing schema from `src/Modules/Authentication/Infrastructure/Database/Schema/`
- Export server instance for use by Next.js route handler

**Better Auth client config:**
- Import `better-auth/client`
- Call `createAuthClient()` with base URL from env
- Export typed client instance

**Composition Root structure:**
- `container.ts` — IoC container class/instance with `bind<T>(token, factory)` and `resolve<T>(token)` methods
- `Bindings/index.ts` — Register all dependencies: BetterAuthService, Repository implementations, UseCases
- `Containers/index.ts` — Factory/initializer for the container
- All Authentication module bindings use `@/` path aliases

**Session utility:**
- Read request headers from Next.js
- Call Better Auth server instance `getSession()` with headers
- Return typed session or null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire Better Auth server config</name>
  <files>src/Lib/BetterAuth/Config/server.ts</files>
   <read_first>
     - src/Modules/Authentication/Infrastructure/Database/Schema/index.ts (existing Drizzle schema barrel)
     - src/Modules/Authentication/Infrastructure/Database/Schema/user.schema.ts (user table definition)
     - src/Modules/Authentication/Infrastructure/Database/Schema/session.schema.ts (session table definition)
     - src/Modules/Authentication/Infrastructure/Database/Schema/account.schema.ts (account table definition)
     - src/Modules/Authentication/Infrastructure/Database/Schema/verification.schema.ts (verification table definition)
     - src/Modules/Authentication/Infrastructure/Database/Relations/relations.ts (existing Drizzle relations)
     - node_modules/next/dist/docs/ for any deprecation notices about route handlers
   </read_first>
  <action>
    In `src/Lib/BetterAuth/Config/server.ts`:
    - Create a new schema.ts file at `src/Lib/BetterAuth/schema.ts` that re-exports the auth tables from the Drizzle schema directory using `better-auth` table definitions. Better Auth needs its own schema derived from the DB tables.
    - Import `better-auth`, `better-auth/adapters/drizzle`, and the auth schema
    - Import `@better-auth/email-password` plugin
    - Configure Drizzle adapter pointing to the DB connection from `src/Lib/db/index.ts` (or the app's drizzle client)
    - Create `auth()` with: `emailAndPassword` plugin, `secret` from `BETTER_AUTH_SECRET` env var, `baseURL` from `BETTER_AUTH_URL` env var
    - Export the `auth` instance as default
    - Export type `AuthServer` from the return type of `auth()`
  </action>
  <acceptance_criteria>
    - `src/Lib/BetterAuth/Config/server.ts` exports a configured `auth()` instance
    - Drizzle adapter is wired to the existing schema tables
    - `emailAndPassword` plugin is registered in the plugins array
    - `secret` and `baseURL` are read from environment variables
    - TypeScript compiles with `npx tsc --noEmit` (no Better Auth type errors)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Better Auth client config</name>
  <files>src/Lib/BetterAuth/Config/client.ts</files>
  <read_first>
    - src/Lib/BetterAuth/Config/server.ts (server instance for type reference)
  </read_first>
  <action>
    In `src/Lib/BetterAuth/Config/client.ts`:
    - Import `createAuthClient` from `better-auth/client`
    - Call `createAuthClient()` with `baseURL: process.env.NEXT_PUBLIC_BASE_URL!`
    - Export the client instance
    - Export the inferred `AuthClient` type
  </action>
  <acceptance_criteria>
    - `src/Lib/BetterAuth/Config/client.ts` exports a configured client instance
    - Base URL is read from `NEXT_PUBLIC_BASE_URL` env var
    - TypeScript compiles clean
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Better Auth config barrel</name>
  <files>src/Lib/BetterAuth/Config/index.ts</files>
  <read_first>
    - src/Lib/BetterAuth/Config/server.ts (exports to re-export)
    - src/Lib/BetterAuth/Config/client.ts (exports to re-export)
  </read_first>
  <action>
    In `src/Lib/BetterAuth/Config/index.ts`:
    - Re-export server auth instance and type
    - Re-export client auth instance and type
    - Add `export type *` for all types from both files
  </action>
  <acceptance_criteria>
    - Barrel re-exports both server and client instances and types
    - Imports from `@/Lib/BetterAuth/Config` resolve both server and client exports
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create session utility</name>
  <files>src/Lib/BetterAuth/utils/get-session.ts</files>
  <read_first>
    - src/Lib/BetterAuth/Config/server.ts (auth instance for session API)
    - node_modules/better-auth/dist/types (session return types)
  </read_first>
  <action>
    In `src/Lib/BetterAuth/utils/get-session.ts`:
    - Import the server auth instance from `@/Lib/BetterAuth/Config/server`
    - Create `getSession(headers: Headers)` function that calls `auth.api.getSession({ headers })`
    - Return typed session result or null
    - Handle the Better Auth session response format (may wrap in result type)
  </action>
  <acceptance_criteria>
    - `getSession` function accepts `Headers` argument
    - Function calls `auth.api.getSession` with the headers
    - Returns typed session data or null
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create Composition Root container</name>
  <files>
    src/CompositionRoot/container.ts
    src/CompositionRoot/Bindings/index.ts
    src/CompositionRoot/Containers/index.ts
  </files>
  <read_first>
    - src/CompositionRoot/container.ts (current state — EMPTY)
    - src/CompositionRoot/Bindings/index.ts (current state — EMPTY)
    - src/CompositionRoot/Containers/index.ts (current state — EMPTY)
    - CONTEXT.md D-02 for Composition Root requirements
  </read_first>
  <action>
    Create a lightweight DI container:

    In `src/CompositionRoot/container.ts`:
    - Define `ContainerToken<T>` branded type for type-safe tokens
    - Create `Container` class with:
      - Private `bindings: Map<string, { factory: () => unknown; singleton: boolean; instance?: unknown }>`
      - `bind<T>(token: ContainerToken<T>, factory: () => T, singleton?: boolean): void`
      - `resolve<T>(token: ContainerToken<T>): T` — returns instance, creates if singleton not yet instantiated
      - `isBound<T>(token: ContainerToken<T>): boolean`
    - Export singleton `container` instance
    - Export `getContainer()` factory for testing

    In `src/CompositionRoot/Bindings/index.ts`:
    - Define container tokens for each Authentication module dependency
    - Register tokens for: IAuthRepository (Infrastructure), ISessionRepository, SignInUseCase, SignUpUseCase, SignOutUseCase, GetSessionQuery
    - Each binding uses lazy singleton instantiation via factory functions
    - Import concrete implementations from their respective module paths

    In `src/CompositionRoot/Containers/index.ts`:
    - Export the container instance and getContainer function
    - Export all container token types for use by Server Actions
  </action>
  <acceptance_criteria>
    - `container.ts` exports a functional DI container with bind/resolve/isBound
    - `Bindings/index.ts` registers all Authentication module dependencies
    - `Containers/index.ts` provides clean public API
    - TypeScript compiles clean with `npx tsc --noEmit`
    - Container can resolve a registered binding and return the correct type
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Client config | Client Better Auth config exposes base URL — acceptable public info |
| Server → Drizzle adapter | Drizzle adapter connects to PostgreSQL with connection string from env |
| Composition Root → Service locator | Container acts as service locator — not injected, but acceptable for a framework-integration layer |

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-01 | Info Disclosure | client.ts | accept | Base URL is public info required for client auth |
| T-08-02 | Tampering | server.ts | mitigate | Secret read from env var, never hardcoded; validate env at startup |
</threat_model>

<verification>
- `npx tsc --noEmit` succeeds with no errors in Better Auth files or Composition Root
- `src/Lib/BetterAuth/Config/server.ts` exports a configured auth() instance
- `src/Lib/BetterAuth/Config/client.ts` exports a configured client instance
- `src/CompositionRoot/container.ts` can bind and resolve a test token
</verification>

<success_criteria>
1. Better Auth server config with Drizzle adapter and email/password plugin is complete
2. Better Auth client config is wired with env-based base URL
3. Session utility extracts current session from request headers
4. Composition Root container.ts has bind/resolve/isBound API with all Authentication bindings registered
5. All files type-check with `npx tsc --noEmit`
</success_criteria>

<output>
Create SUMMARY.md when done: `.planning/phases/08-module-integration-server-actions/08-01-SUMMARY.md`
</output>
