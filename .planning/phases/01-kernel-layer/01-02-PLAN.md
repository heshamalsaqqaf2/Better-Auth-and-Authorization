---
phase: 01-kernel-layer
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/Core/Kernel/Contracts/Base/error-base.contract.ts
  - src/Core/Kernel/Contracts/Base/result-base.contract.ts
  - src/Core/Kernel/Contracts/Base/index.ts
  - src/Core/Kernel/Contracts/Validators/layer-validator.contract.ts
  - src/Core/Kernel/Contracts/Validators/error-validator.contract.ts
  - src/Core/Kernel/Contracts/Validators/result-validator.contract.ts
  - src/Core/Kernel/Contracts/Validators/index.ts
  - src/Core/Kernel/Contracts/index.ts
  - src/Core/Kernel/Constants/error-codes.constants.ts
  - src/Core/Kernel/Constants/layer-names.constants.ts
  - src/Core/Kernel/Constants/index.ts
  - src/Core/Kernel/index.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CNST-01, CNST-02, API-01, API-02]
user_setup: []

must_haves:
  truths:
    - "Developer can implement the ErrorBase contract interface on error classes with all required properties (code, message, timestamp, layer, cause) and methods (isRecoverable, getSeverity, toJSON)"
    - "Developer can implement the ResultBase<T, E> contract interface with monadic operations: map, flatMap, mapError, match, fold, tap, tapError"
    - "Developer can use LayerValidator<T>, ErrorValidator<T>, and ResultValidator<T> contract interfaces for runtime compliance checking"
    - "Developer can reference ErrorCodes as const object and its derived ErrorCode union type"
    - "Developer can reference LayerNames as const object mapping LayerType keys to human-readable strings"
    - "Developer can import ALL Kernel exports (primitives + contracts + constants) from a single @/Core/Kernel import"
  artifacts:
    - path: "src/Core/Kernel/Contracts/Base/error-base.contract.ts"
      provides: "ErrorBase interface — code, message, timestamp, layer, cause?, isRecoverable(), getSeverity(), toJSON()"
      contains: "export interface ErrorBase"
    - path: "src/Core/Kernel/Contracts/Base/result-base.contract.ts"
      provides: "ResultBase<T, E extends ErrorBase> interface — isSuccess, isFailure, data?, error?, map, flatMap, mapError, match, fold, tap, tapError"
      contains: "export interface ResultBase"
    - path: "src/Core/Kernel/Contracts/Validators/layer-validator.contract.ts"
      provides: "ValidationResult interface + LayerValidator<T> interface"
      contains: "export interface LayerValidator"
    - path: "src/Core/Kernel/Contracts/Validators/error-validator.contract.ts"
      provides: "ErrorValidator<T> interface"
      contains: "export interface ErrorValidator"
    - path: "src/Core/Kernel/Contracts/Validators/result-validator.contract.ts"
      provides: "ResultValidator<T> interface"
      contains: "export interface ResultValidator"
    - path: "src/Core/Kernel/Constants/error-codes.constants.ts"
      provides: "ErrorCodes as const object with derived ErrorCode union type"
      contains: "export const ErrorCodes"
    - path: "src/Core/Kernel/Constants/layer-names.constants.ts"
      provides: "LayerNames as const object mapping LayerType keys to human-readable strings, derived LayerName union type"
      contains: "export const LayerNames"
    - path: "src/Core/Kernel/index.ts"
      provides: "Updated top-level barrel exporting Primitives + Contracts + Constants"
      contains: "export * from './Primitives', './Contracts', './Constants'"
  key_links:
    - from: "ErrorBase.cause"
      to: "ErrorBase"
      via: "Optional recursive reference for cause chains — cause?: ErrorBase"
      pattern: "cause?"
    - from: "ResultBase<E extends ErrorBase>"
      to: "ErrorBase interface"
      via: "Generic constraint — E must extend ErrorBase"
      pattern: "extends ErrorBase"
    - from: "LayerNames"
      to: "LayerType enum"
      via: "Keys derived from LayerType object — satisfies Record<keyof typeof LayerType, string>"
      pattern: "satisfies Record<keyof typeof LayerType"
    - from: "src/Core/Kernel/index.ts"
      to: "Primitives, Contracts, Constants"
      via: "Top-level barrel re-exporting all three subsections"
      pattern: "export \\* from './"
---

<objective>
**Deliver contract interfaces (ErrorBase, ResultBase, Validators) and constants (ErrorCodes, LayerNames), then complete the barrel export chain so all Kernel exports are available from a single import.**

Purpose: After this plan, developers can implement the ErrorBase and ResultBase contracts on concrete classes, use validator contracts for runtime shape checking, reference standardized error code and layer name constants, and import everything from `@/Core/Kernel`.

This plan completes the public API surface for the Kernel layer.

Output:
- `error-base.contract.ts`, `result-base.contract.ts` — core contract interfaces
- `layer-validator.contract.ts`, `error-validator.contract.ts`, `result-validator.contract.ts` — validator contracts
- `error-codes.constants.ts`, `layer-names.constants.ts` — constants
- Full barrel chain: Base → Contracts, Validators → Contracts, Constants, and updated top-level Kernel/index.ts
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/01-kernel-layer/01-RESEARCH.md (lines 271-354 for contract patterns, lines 471-592 for code examples)
@.planning/phases/01-kernel-layer/01-PATTERNS.md (lines 240-368 for exact code templates)
@.planning/phases/01-kernel-layer/01-CONTEXT.md (lines 24-55 for locked decisions)
@.planning/phases/01-kernel-layer/SKELETON.md

<interfaces>
<!-- Key types the contracts depend on — imported from Primitives (already created in Plan 01-01). -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/Core/Kernel/Primitives/Enums/severity.enum.ts:
```typescript
export const Severity = { CRITICAL: 'critical', ERROR: 'error', WARNING: 'warning', INFO: 'info' } as const;
export type Severity = (typeof Severity)[keyof typeof Severity];
export const SEVERITY_VALUES: readonly Severity[] = Object.values(Severity);
```

From src/Core/Kernel/Primitives/Enums/layer-type.enum.ts:
```typescript
export const LayerType = { DOMAIN: 'domain', APPLICATION: 'application', INFRASTRUCTURE: 'infrastructure', PRESENTATION: 'presentation' } as const;
export type LayerType = (typeof LayerType)[keyof typeof LayerType];
export const LAYER_TYPE_VALUES: readonly LayerType[] = Object.values(LayerType);
```

**Naming rule (from PATTERNS.md line 78):**
The branded `ErrorCode` type is exported from Primitives as `BrandedErrorCode`. The constants-derived `ErrorCode` comes from `error-codes.constants.ts`. These are distinct — contracts use `string` for `code` properties, not branded types, so no conflict arises in contract code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core contract interfaces (ErrorBase, ResultBase) + base barrel</name>
  <files>
    - src/Core/Kernel/Contracts/Base/error-base.contract.ts
    - src/Core/Kernel/Contracts/Base/result-base.contract.ts
    - src/Core/Kernel/Contracts/Base/index.ts
  </files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Base\error-base.contract.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Base\result-base.contract.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Base\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 240-295 for exact code templates)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-RESEARCH.md (lines 471-511 for code examples)
  </read_first>
  <acceptance_criteria>
    1. `error-base.contract.ts` exports `ErrorBase` interface with: `readonly code: string`, `readonly message: string`, `readonly timestamp: Date`, `readonly layer: LayerType`, `readonly cause?: ErrorBase`, `isRecoverable(): boolean`, `getSeverity(): Severity`, `toJSON(): Record<string, unknown>`
    2. `error-base.contract.ts` imports `Severity` from `../../Primitives/Enums/severity.enum` and `LayerType` from `../../Primitives/Enums/layer-type.enum` using RELATIVE paths
    3. `result-base.contract.ts` exports `ResultBase<T, E extends ErrorBase>` generic interface with: `readonly isSuccess: boolean`, `readonly isFailure: boolean`, `readonly data?: T`, `readonly error?: E`, `map<U>(fn: (data: T) => U): ResultBase<U, E>`, `flatMap<U>(fn: (data: T) => ResultBase<U, E>): ResultBase<U, E>`, `mapError<F extends ErrorBase>(fn: (error: E) => F): ResultBase<T, F>`, `match<R>(handlers: { onSuccess: (data: T) => R; onFailure: (error: E) => R }): R`, `fold<R>(onSuccess: (data: T) => R, onFailure: (error: E) => R): R`, `tap(fn: (data: T) => void): ResultBase<T, E>`, `tapError(fn: (error: E) => void): ResultBase<T, E>`
    4. `result-base.contract.ts` imports `ErrorBase` from `./error-base.contract` using RELATIVE paths
    5. `Contracts/Base/index.ts` re-exports both interfaces with `export type`
    6. `npx tsc --noEmit` passes after writing all three files
  </acceptance_criteria>
  <action>
    Per D-08 (ErrorBase contract per Base_System.md §2.1), D-09 (ResultBase contract per Base_System.md §3.1), D-16 (zero external dependencies), D-18 (naming convention `.contract.ts`):

    **error-base.contract.ts**: Per PATTERNS.md lines 251-270. Import `Severity` and `LayerType` types from the Primitives enums using RELATIVE imports (`../../Primitives/Enums/severity.enum`, `../../Primitives/Enums/layer-type.enum`). Define `ErrorBase` interface with: `code`, `message`, `timestamp`, `layer`, `cause?: ErrorBase` (optional per discretion — RESEACH.md line 42), `isRecoverable()`, `getSeverity()`, `toJSON()`. All properties `readonly`. Type imports use `import type { ... }` for `verbatimModuleSyntax` compliance.

    **result-base.contract.ts**: Per PATTERNS.md lines 273-294. Import `ErrorBase` type from `./error-base.contract`. Define `ResultBase<T, E extends ErrorBase>` generic interface with: discriminant accessors (`isSuccess`, `isFailure`, `data?`, `error?`), monadic methods (`map`, `flatMap`, `mapError`, `match`, `fold`, `tap`, `tapError`) with exact signatures from PATTERNS.md.
    
    Per discretion from CONTEXT.md: include `mapError` (RESEARCH.md line 41 recommends inclusion). Make `cause` optional (`cause?: ErrorBase`) per RESEARCH.md line 42.

    **Contracts/Base/index.ts**: Re-export both as types: `export type { ErrorBase } from './error-base.contract'` and `export type { ResultBase } from './result-base.contract'`.

    After writing, run `npx tsc --noEmit` and confirm zero type errors.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — core contracts compile" }
    </automated>
  </verify>
  <done>
    ErrorBase and ResultBase interfaces exist with all required properties and methods, using relative imports from Primitives. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create validator contract interfaces + validators barrel + contracts barrel</name>
  <files>
    - src/Core/Kernel/Contracts/Validators/layer-validator.contract.ts
    - src/Core/Kernel/Contracts/Validators/error-validator.contract.ts
    - src/Core/Kernel/Contracts/Validators/result-validator.contract.ts
    - src/Core/Kernel/Contracts/Validators/index.ts
    - src/Core/Kernel/Contracts/index.ts
  </files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Validators\layer-validator.contract.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Validators\error-validator.contract.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Validators\result-validator.contract.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\Validators\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Contracts\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 297-326 for exact code templates, lines 87-98 for barrel patterns)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-RESEARCH.md (lines 514-540 for code examples)
  </read_first>
  <acceptance_criteria>
    1. `layer-validator.contract.ts` exports `ValidationResult` interface with `readonly isValid: boolean` and `readonly errors: readonly string[]`, plus `LayerValidator<T>` interface with `validate(value: unknown): ValidationResult` and `belongsToLayer(value: unknown): boolean`
    2. `error-validator.contract.ts` exports `ErrorValidator<T>` interface with `validate(error: unknown): ValidationResult` and `satisfiesContract(error: unknown): boolean`
    3. `result-validator.contract.ts` exports `ResultValidator<T>` interface with `validate(result: unknown): ValidationResult` and `satisfiesContract(result: unknown): boolean`
    4. `Validators/index.ts` re-exports all three validator interfaces plus `ValidationResult`
    5. `Contracts/index.ts` re-exports both `./Base` and `./Validators` via `export *`
    6. `npx tsc --noEmit` passes after writing all five files
  </acceptance_criteria>
  <action>
    Per D-10 (validator contracts per Base_System.md §5.3), D-18 (naming convention `.contract.ts`):

    **layer-validator.contract.ts**: Per PATTERNS.md lines 297-326. Define `ValidationResult` interface (isValid + errors) and `LayerValidator<T>` generic interface with `validate(value: unknown): ValidationResult` and `belongsToLayer(value: unknown): boolean`. No imports needed — zero dependencies.

    **error-validator.contract.ts**: Per PATTERNS.md lines 313-318. Import `ValidationResult` from `./layer-validator.contract`. Define `ErrorValidator<T>` interface with `validate(error: unknown): ValidationResult` and `satisfiesContract(error: unknown): boolean`.

    **result-validator.contract.ts**: Per PATTERNS.md lines 320-325. Import `ValidationResult` from `./layer-validator.contract`. Define `ResultValidator<T>` interface with `validate(result: unknown): ValidationResult` and `satisfiesContract(result: unknown): boolean`.

    Keep validator contracts minimal per Pitfall 5 (RESEARCH.md lines 403-406) — interfaces only, no implementations. Use `import type { ... }` for `verbatimModuleSyntax` compliance.

    **Validators/index.ts**: Per PATTERNS.md lines 91-94. Re-export all three validators plus ValidationResult with `export type`.

    **Contracts/index.ts**: Per PATTERNS.md lines 96-98. `export * from './Base'` and `export * from './Validators'`.

    After writing, run `npx tsc --noEmit` and confirm zero type errors.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — validator contracts compile" }
    </automated>
  </verify>
  <done>
    All three validator interfaces exist with minimal signatures. ValidationResult is shared. Validators barrel and Contracts barrel re-export correctly. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create constants (ErrorCodes, LayerNames) + constants barrel + update top-level barrel</name>
  <files>
    - src/Core/Kernel/Constants/error-codes.constants.ts
    - src/Core/Kernel/Constants/layer-names.constants.ts
    - src/Core/Kernel/Constants/index.ts
    - src/Core/Kernel/index.ts
  </files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Constants\error-codes.constants.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Constants\layer-names.constants.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Constants\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\index.ts (already created in Plan 01-01 — MUST read before modifying)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 328-368 for exact code templates, lines 99-103 for Constants barrel, lines 119-128 for top-level barrel)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-RESEARCH.md (lines 542-582 for code examples)
  </read_first>
  <acceptance_criteria>
    1. `error-codes.constants.ts` exports `ErrorCodes` as const object (empty — no domain-specific codes yet) with derived `ErrorCode` union type (evaluates to `never`)
    2. `layer-names.constants.ts` exports `LayerNames` as const object with keys `DOMAIN`, `APPLICATION`, `INFRASTRUCTURE`, `PRESENTATION` mapped to `'Domain Layer'`, `'Application Layer'`, `'Infrastructure Layer'`, `'Presentation Layer'` using `satisfies Record<keyof typeof LayerType, string>`, plus derived `LayerName` union type
    3. `Constants/index.ts` re-exports both: `export { ErrorCodes, type ErrorCode } from './error-codes.constants'` and `export { LayerNames, type LayerName } from './layer-names.constants'`
    4. `src/Core/Kernel/index.ts` updated to export all three sections: `export * from './Primitives'`, `export * from './Contracts'`, `export * from './Constants'`
    5. Full barrel hierarchy is valid: Primitives → Enums + Types, Contracts → Base + Validators, Constants → both files
    6. `npx tsc --noEmit` passes with zero type errors across the entire Kernel
    7. All 14 phase requirement IDs compile successfully (PRIM-01..05, CONT-01..05, CNST-01..02, API-01..02)
  </acceptance_criteria>
  <action>
    Per D-11 (error codes as as const object), D-12 (layer names as as const object), D-13 (barrel exports at each subdirectory), D-14 (top-level barrel from src/Core/Kernel/index.ts), D-18 (naming convention `.constants.ts`):

    **error-codes.constants.ts**: Per PATTERNS.md lines 334-348. Export `ErrorCodes` as const empty object with JSDoc noting "reserved for future domain codes — Phase 3". Export derived `ErrorCode` union type. Import nothing — zero dependencies. The derived type evaluates to `never` since the object is empty.

    **layer-names.constants.ts**: Per PATTERNS.md lines 350-368. Import `LayerType` from `../Primitives/Enums/layer-type.enum`. Export `LayerNames` as const object with keys `DOMAIN`, `APPLICATION`, `INFRASTRUCTURE`, `PRESENTATION` mapped to human-readable strings `'Domain Layer'`, `'Application Layer'`, `'Infrastructure Layer'`, `'Presentation Layer'`. Use `satisfies Record<keyof typeof LayerType, string>` to ensure key alignment with LayerType. Export derived `LayerName` union type. Use `import type { ... }` for `verbatimModuleSyntax` compliance.

    **Constants/index.ts**: Per PATTERNS.md lines 99-103. Re-export both constants objects and their types.

    **src/Core/Kernel/index.ts**: READ the existing file first (created in Plan 01-01). INSERT `export * from './Contracts'` and `export * from './Constants'` after the existing `export * from './Primitives'` line. Do NOT delete or restructure existing exports.

    After writing, run `npx tsc --noEmit` and confirm zero type errors across the entire Kernel layer.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — full Kernel compiles" }
    </automated>
  </verify>
  <done>
    ErrorCodes and LayerNames constants exist with correct types. All barrels complete. Top-level Kernel/index.ts exports all three sections. `npx tsc --noEmit` passes with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 1 has zero runtime behavior — no I/O, no data handling, no network boundaries. All artifacts are compile-time types and interfaces. No trust boundaries exist in this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Contract interfaces | mitigate | Interfaces are type-only — no runtime behavior to subvert. Compile-time verification via `npx tsc --noEmit`. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed in Phase 1 — no package legitimacy gate needed |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` — must pass with zero type errors across full Kernel
2. Verify barrel export chain: `import { ErrorBase, ResultBase, LayerValidator } from '@/Core/Kernel'` would resolve all types (compile-time only)
3. Verify `import { ErrorCodes, LayerNames } from '@/Core/Kernel'` would resolve both constants
4. Verify all 14 requirement compilations succeed (PRIM-01..05, CONT-01..05, CNST-01..02, API-01..02)
</verification>

<success_criteria>
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `error-base.contract.ts` and `result-base.contract.ts` exported with full contracts
- [ ] All 3 validator contracts (`layer-validator.contract.ts`, `error-validator.contract.ts`, `result-validator.contract.ts`) exported
- [ ] `error-codes.constants.ts` exists with empty `ErrorCodes` object and derived `ErrorCode` type
- [ ] `layer-names.constants.ts` exists with `LayerNames` mapping LayerType keys to human-readable strings
- [ ] `src/Core/Kernel/index.ts` exports Primitives, Contracts, and Constants
- [ ] All 14 phase requirement IDs compile: PRIM-01..05, CONT-01..05, CNST-01..02, API-01..02
- [ ] Zero external dependencies in any Kernel file
</success_criteria>

<output>
Create `.planning/phases/01-kernel-layer/01-02-SUMMARY.md` when done
</output>
