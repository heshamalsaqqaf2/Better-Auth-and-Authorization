---
phase: 01-kernel-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - src/Core/Kernel/Primitives/Enums/severity.enum.ts
  - src/Core/Kernel/Primitives/Enums/layer-type.enum.ts
  - src/Core/Kernel/Primitives/Enums/index.ts
  - src/Core/Kernel/Primitives/Types/correlation-id.type.ts
  - src/Core/Kernel/Primitives/Types/error-code.type.ts
  - src/Core/Kernel/Primitives/Types/operation-id.type.ts
  - src/Core/Kernel/Primitives/Types/index.ts
  - src/Core/Kernel/Primitives/index.ts
  - src/Core/Kernel/index.ts
autonomous: true
requirements: [PRIM-01, PRIM-02, PRIM-03, PRIM-04, PRIM-05]
user_setup: []

must_haves:
  truths:
    - "Developer can import Severity from the Kernel barrel and classify errors by severity level"
    - "Developer can import LayerType from the Kernel barrel and classify errors by origin layer"
    - "Developer can create CorrelationId branded values via createCorrelationId() — invalid empty strings throw TypeError"
    - "Developer can create ErrorCode branded values via createErrorCode() — invalid empty strings throw TypeError"
    - "Developer can create OperationId branded values via createOperationId() — invalid empty strings throw TypeError"
    - "All primitives are importable from a single @/Core/Kernel import (via the top-level barrel)"
  artifacts:
    - path: "tsconfig.json"
      provides: "ES2022 target + strictness flags required by branded types and contract patterns"
      contains: "target: ES2022"
    - path: "src/Core/Kernel/Primitives/Enums/severity.enum.ts"
      provides: "Severity as const object, Severity type union, SEVERITY_VALUES runtime array"
      contains: "export const Severity"
    - path: "src/Core/Kernel/Primitives/Enums/layer-type.enum.ts"
      provides: "LayerType as const object, LayerType type union, LAYER_TYPE_VALUES runtime array"
      contains: "export const LayerType"
    - path: "src/Core/Kernel/Primitives/Types/correlation-id.type.ts"
      provides: "CorrelationId branded type, createCorrelationId factory with validation, isCorrelationId type guard"
      contains: "createCorrelationId"
    - path: "src/Core/Kernel/Primitives/Types/error-code.type.ts"
      provides: "ErrorCode branded type, createErrorCode factory with validation, isErrorCode type guard"
      contains: "createErrorCode"
    - path: "src/Core/Kernel/Primitives/Types/operation-id.type.ts"
      provides: "OperationId branded type, createOperationId factory with validation, isOperationId type guard"
      contains: "createOperationId"
    - path: "src/Core/Kernel/Primitives/Enums/index.ts"
      provides: "Barrel re-exporting both enums"
      contains: "export { Severity"
    - path: "src/Core/Kernel/Primitives/Types/index.ts"
      provides: "Barrel re-exporting all 3 branded types (ErrorCode renamed to BrandedErrorCode for top-level clarity)"
      contains: "BrandedErrorCode"
    - path: "src/Core/Kernel/Primitives/index.ts"
      provides: "Barrel re-exporting Enums and Types subdirectories"
      contains: "export * from './Enums'"
    - path: "src/Core/Kernel/index.ts"
      provides: "Initial top-level barrel exporting Primitives section"
      contains: "export * from './Primitives'"
  key_links:
    - from: "Factory functions (createCorrelationId, createErrorCode, createOperationId)"
      to: "tsconfig.json target ES2022"
      via: "unique symbol brand declaration requires ES2015+ target"
      pattern: "unique symbol"
    - from: "src/Core/Kernel/Primitives/Types/error-code.type.ts (branded ErrorCode)"
      to: "src/Core/Kernel/Constants/error-codes.constants.ts (constant ErrorCode)"
      via: "Name collision — branded type exported as BrandedErrorCode in barrel to disambiguate"
      pattern: "BrandedErrorCode"
    - from: "src/Core/Kernel/Primitives/Types/index.ts"
      to: "src/Core/Kernel/Primitives/index.ts"
      via: "Barrel re-export chain: Types/index → Primitives/index → Kernel/index"
      pattern: "export"
---

<objective>
**Deliver typed primitives (enums + branded types) that define the Kernel's vocabulary — Severity, LayerType, CorrelationId, ErrorCode, and OperationId — with zero external dependencies.**

Purpose: Establish the type-level foundation. Every error in the system needs a severity classification (critical/error/warning/info) and a layer tag (domain/application/infrastructure/presentation). Every unique identifier (correlation ID, error code, operation ID) needs compile-time type safety via branded types.

Output:
- `tsconfig.json` upgraded to ES2022 target with strict flags
- `severity.enum.ts`, `layer-type.enum.ts` — as const object enums
- `correlation-id.type.ts`, `error-code.type.ts`, `operation-id.type.ts` — unique-symbol branded types + factories + type guards
- Barrel files at Enums/, Types/, Primitives/ levels
- Initial `src/Core/Kernel/index.ts` top-level barrel exporting Primitives
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/01-kernel-layer/01-RESEARCH.md (lines 216-269 for enum and branded type patterns)
@.planning/phases/01-kernel-layer/01-PATTERNS.md (lines 158-238 for exact code templates)
@.planning/phases/01-kernel-layer/01-CONTEXT.md (lines 24-55 for locked decisions)
@.planning/phases/01-kernel-layer/SKELETON.md

<interfaces>
<!-- Key types and contracts the executor needs. All defined in RESEARCH.md + PATTERNS.md already. -->

**Enum pattern (from PATTERNS.md lines 167-203):**
```
export const Severity = { CRITICAL: 'critical', ERROR: 'error', WARNING: 'warning', INFO: 'info' } as const;
export type Severity = (typeof Severity)[keyof typeof Severity];
export const SEVERITY_VALUES: readonly Severity[] = Object.values(Severity);
```

**Branded type pattern (from PATTERNS.md lines 215-238):**
```
declare const __brand: unique symbol;
type Brand<T, B extends string> = T & { readonly [__brand]: B };
export type CorrelationId = Brand<string, 'CorrelationId'>;
export function createCorrelationId(value: string): CorrelationId { ... }
export function isCorrelationId(value: unknown): value is CorrelationId { ... }
```

**Barrel naming rule (from PATTERNS.md line 78):**
The branded `ErrorCode` type must be exported as `BrandedErrorCode` in the barrel to avoid collision with the constants-derived `ErrorCode` union type (CNST-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade tsconfig.json target to ES2022 and add strictness flags</name>
  <files>tsconfig.json</files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\tsconfig.json (current state — MUST read before modifying)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 136-156 for exact change specification)
    - D:\projects\NextJS\authentication-better-auth\docs\Architecture.md (if it exists — check for any build target constraints)
  </read_first>
  <acceptance_criteria>
    1. tsconfig.json `compilerOptions.target` changes from `"ES2017"` to `"ES2022"`
    2. tsconfig.json `compilerOptions` adds `"noUncheckedIndexedAccess": true`
    3. tsconfig.json `compilerOptions` adds `"noImplicitOverride": true`
    4. tsconfig.json `compilerOptions` adds `"verbatimModuleSyntax": true`
    5. tsconfig.json `compilerOptions` adds `"exactOptionalPropertyTypes": true`
    6. `npx tsc --noEmit` passes with zero type errors after change (no code changes yet, so it should pass)
  </acceptance_criteria>
  <action>
    Per D-15 (ES2022 target) and STATE.md recommendation: read the current tsconfig.json, then change `target` from `"ES2017"` to `"ES2022"`. Add these four strictness flags under compilerOptions after the existing `"strict": true` line: `"noUncheckedIndexedAccess": true`, `"noImplicitOverride": true`, `"verbatimModuleSyntax": true`, `"exactOptionalPropertyTypes": true`. Preserve all other existing compiler options exactly.

    Do NOT change `module`, `moduleResolution`, `jsx`, or any other existing setting. Do NOT add rules that affect import syntax without checking Next.js compatibility first.

    After editing, run `npx tsc --noEmit` and confirm zero type errors.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — target ES2022 confirmed" }
    </automated>
  </verify>
  <done>
    tsconfig.json target is ES2022 with noUncheckedIndexedAccess, noImplicitOverride, verbatimModuleSyntax, exactOptionalPropertyTypes added. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create as const enums (Severity, LayerType) with values arrays + enum barrel</name>
  <files>
    - src/Core/Kernel/Primitives/Enums/severity.enum.ts
    - src/Core/Kernel/Primitives/Enums/layer-type.enum.ts
    - src/Core/Kernel/Primitives/Enums/index.ts
  </files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Enums\severity.enum.ts (empty scaffold — confirm emptiness)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Enums\layer-type.enum.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Enums\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 158-203 for exact code templates)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-RESEARCH.md (lines 411-443 for enum pattern with JSDoc guidance)
  </read_first>
  <acceptance_criteria>
    1. `severity.enum.ts` exports `Severity` as const object with keys `CRITICAL`, `ERROR`, `WARNING`, `INFO` mapped to string values `'critical'`, `'error'`, `'warning'`, `'info'`
    2. `severity.enum.ts` exports `Severity` as a derived union type `'critical' | 'error' | 'warning' | 'info'`
    3. `severity.enum.ts` exports `SEVERITY_VALUES: readonly Severity[]` for runtime iteration
    4. `layer-type.enum.ts` exports `LayerType` as const object with keys `DOMAIN`, `APPLICATION`, `INFRASTRUCTURE`, `PRESENTATION` mapped to string values `'domain'`, `'application'`, `'infrastructure'`, `'presentation'`
    5. `layer-type.enum.ts` exports `LayerType` as a derived union type `'domain' | 'application' | 'infrastructure' | 'presentation'`
    6. `layer-type.enum.ts` exports `LAYER_TYPE_VALUES: readonly LayerType[]` for runtime iteration
    7. `Enums/index.ts` re-exports both enums with proper type/value distinction
    8. `npx tsc --noEmit` passes after creating all three files
  </acceptance_criteria>
  <action>
    Per D-01 (as const objects not TypeScript enum), D-02 (Severity values), D-03 (LayerType values):

    **severity.enum.ts**: Write the `Severity` as const object per PATTERNS.md lines 167-184. Include the `SEVERITY_VALUES` array for runtime iteration. No imports needed — zero dependencies.

    **layer-type.enum.ts**: Write the `LayerType` as const object per PATTERNS.md lines 186-203. Include the `LAYER_TYPE_VALUES` array for runtime iteration. No imports needed — zero dependencies.

    **Enums/index.ts**: Write a barrel file per PATTERNS.md lines 72-73. Export both value objects and their types:
    ```
    export { Severity, type Severity, SEVERITY_VALUES } from './severity.enum';
    export { LayerType, type LayerType, LAYER_TYPE_VALUES } from './layer-type.enum';
    ```

    After writing, run `npx tsc --noEmit` to confirm compilation.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — enums compile" }
    </automated>
  </verify>
  <done>
    Severity and LayerType enums exist as as const objects with derived union types and values arrays. Enum barrel re-exports both. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create branded types (CorrelationId, ErrorCode, OperationId) + types barrel + Primitives barrel + top-level barrel</name>
  <files>
    - src/Core/Kernel/Primitives/Types/correlation-id.type.ts
    - src/Core/Kernel/Primitives/Types/error-code.type.ts
    - src/Core/Kernel/Primitives/Types/operation-id.type.ts
    - src/Core/Kernel/Primitives/Types/index.ts
    - src/Core/Kernel/Primitives/index.ts
    - src/Core/Kernel/index.ts
  </files>
  <read_first>
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Types\correlation-id.type.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Types\error-code.type.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Types\operation-id.type.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\Types\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel\Primitives\index.ts (empty scaffold)
    - D:\projects\NextJS\authentication-better-auth\src\Core\Kernel (does index.ts exist? Check — create if not)
    - D:\projects\NextJS\authentication-better-auth\.planning\phases\01-kernel-layer\01-PATTERNS.md (lines 206-238 for branded type code templates, lines 75-84 for Types barrel pattern with BrandedErrorCode rename, lines 83-86 for Primitives barrel, lines 105-128 for top-level barrel)
    - D:\projects\NextJS\authentication-better-auth\src\Lib\Drizzle\index.ts (role analog for barrel style — lines 1-6)
    - D:\projects\NextJS\authentication-better-auth\src\Lib\Drizzle\Database\index.ts (role analog for intermediate barrel style — lines 1-17)
  </read_first>
  <acceptance_criteria>
    1. `correlation-id.type.ts` exports `CorrelationId` branded type, `createCorrelationId(value: string): CorrelationId` factory (throws TypeError on empty string), `isCorrelationId(value: unknown): value is CorrelationId` type guard
    2. `error-code.type.ts` exports `ErrorCode` branded type, `createErrorCode(value: string): ErrorCode` factory (throws TypeError on empty string), `isErrorCode(value: unknown): value is ErrorCode` type guard
    3. `operation-id.type.ts` exports `OperationId` branded type, `createOperationId(value: string): OperationId` factory (throws TypeError on empty string), `isOperationId(value: unknown): value is OperationId` type guard
    4. Each branded type uses the unique-symbol `declare const __brand: unique symbol` + `Brand<T, B>` utility type pattern
    5. Each factory function has exactly ONE `as` cast (the branded return type assertion)
    6. `Types/index.ts` re-exports all three types — branded `ErrorCode` re-exported as `BrandedErrorCode` to avoid name collision with constants-derived `ErrorCode` (CNST-01)
    7. `Primitives/index.ts` re-exports both `./Enums` and `./Types` via `export *`
    8. `src/Core/Kernel/index.ts` (CREATED if absent) exports `./Primitives` via `export *`
    9. `npx tsc --noEmit` passes with zero type errors
  </acceptance_criteria>
  <action>
    Per D-04 (unique-symbol branded types), D-05 (CorrelationId validates non-empty), D-06 (ErrorCode validates non-empty), D-07 (OperationId validates non-empty), D-16 (zero external dependencies), D-18 (naming convention):

    **Each type file** (correlation-id.type.ts, error-code.type.ts, operation-id.type.ts): Follow the unique-symbol branded type pattern from PATTERNS.md lines 215-238. Each file contains:
    - `declare const __brand: unique symbol;` (top of file, module-scoped — each file gets its own unique `__brand`)
    - `type Brand<T, B extends string> = T & { readonly [__brand]: B };` utility type
    - Export branded type (e.g., `export type CorrelationId = Brand<string, 'CorrelationId'>`)
    - Export factory function with non-empty validation: if `value.length === 0`, throw `TypeError`; else `return value as CorrelationId` (single as cast)
    - Export type guard: `typeof value === 'string' && value.length > 0`

    **Types/index.ts**: Per PATTERNS.md lines 75-84. Re-export all three. The branded `ErrorCode` type MUST be exported as `BrandedErrorCode` (with `export type { ErrorCode as BrandedErrorCode }`) to avoid collision with the constants-derived `ErrorCode` union type from CNST-01.

    **Primitives/index.ts**: Per PATTERNS.md lines 83-86. `export * from './Enums'; export * from './Types';`

    **src/Core/Kernel/index.ts**: Check if the file exists (it should not per CONTEXT.md). If absent, create it with:
    ```
    export * from './Primitives';
    ```
    Do NOT add Contracts or Constants yet — those come in Plan 01-02. Use the barrel style analog from `src/Lib/Drizzle/index.ts` (minimal, clean re-exports).

    After writing all 6 files, run `npx tsc --noEmit` and confirm zero type errors.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit 2>&1
      if ($?) { "tsc passed — all primitives compile" }
    </automated>
  </verify>
  <done>
    All 3 branded types exist with factory functions, type guards, and unique-symbol branding. All barrel files at Types, Primitives, and Kernel levels re-export correctly. `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 1 has zero runtime behavior — no I/O, no data handling, no network boundaries. All artifacts are compile-time types and interfaces. No trust boundaries exist in this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tsconfig.json | mitigate | Only modify `target` and add strict flags per specification; executor reads file before modification |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed in Phase 1 — no package legitimacy gate needed |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` — must pass with zero type errors
2. Verify Severity and LayerType are importable as both values and types
3. Verify CorrelationId, ErrorCode (as BrandedErrorCode), OperationId types and factory functions are exportable
4. Verify empty-string validation: `createCorrelationId('')` throws TypeError
</verification>

<success_criteria>
- [ ] `tsconfig.json` has `"target": "ES2022"` and the four strictness flags
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `severity.enum.ts` and `layer-type.enum.ts` export const objects with derived union types and values arrays
- [ ] `correlation-id.type.ts`, `error-code.type.ts`, `operation-id.type.ts` export branded types, factory functions (throw on empty), and type guards
- [ ] `Types/index.ts` exports branded `ErrorCode` as `BrandedErrorCode` to avoid name collision
- [ ] `Primitives/index.ts` and `Kernel/index.ts` barrels re-export correctly
- [ ] Developer can `import { Severity, CorrelationId, createCorrelationId } from '@/Core/Kernel'`
</success_criteria>

<output>
Create `.planning/phases/01-kernel-layer/01-01-SUMMARY.md` when done
</output>
