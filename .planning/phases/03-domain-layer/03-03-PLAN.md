---
phase: 03-domain-layer
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/Core/Foundations/Domain/Errors/Specific/index.ts
  - src/Core/Foundations/Domain/Errors/index.ts
  - src/Core/Foundations/Domain/Results/index.ts
  - src/Core/Foundations/Domain/index.ts
files_deleted:
  - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/
  - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/complaint-not-found.error.ts
  - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/complaint-already-exists.error.ts
  - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/index.ts
  - src/Core/Foundations/Domain/Errors/Specific/UserErrors/
  - src/Core/Foundations/Domain/Errors/Specific/UserErrors/user-not-found.error.ts
  - src/Core/Foundations/Domain/Errors/Specific/UserErrors/index.ts
  - src/Core/Foundations/Domain/Results/domain-result.contract.ts
  - src/Core/Foundations/Domain/Results/domain-result.type-guard.ts
  - src/Core/Foundations/Domain/Results/domain-result.validator.ts
autonomous: true
requirements: [DOMN-01, DOMN-02, DOMN-03]

must_haves:
  truths:
    - "Developer can import all specific domain errors from Domain/Errors/Specific/index.ts"
    - "Developer can import all domain errors (DomainError + specific errors) from Domain/Errors/index.ts"
    - "Developer can import DomainResult from Domain/Results/index.ts"
    - "Developer can import all domain types from Domain/index.ts (contracts, errors, results)"
    - "Domain/index.ts re-exports exactly: contracts, errors, results — no Base re-exports"
    - "Old ComplaintErrors/ and UserErrors/ subdirectories are deleted — all specific errors are flat in Specific/"
    - "Unused stub files (domain-result.contract.ts, domain-result.type-guard.ts, domain-result.validator.ts) are deleted"
  artifacts:
    - path: "src/Core/Foundations/Domain/Errors/Specific/index.ts"
      provides: "Barrel re-exporting all three specific domain errors"
      min_lines: 3
    - path: "src/Core/Foundations/Domain/Errors/index.ts"
      provides: "Barrel re-exporting DomainError and Specific errors"
      min_lines: 4
    - path: "src/Core/Foundations/Domain/Results/index.ts"
      provides: "Barrel re-exporting DomainResult type alias"
      min_lines: 1
    - path: "src/Core/Foundations/Domain/index.ts"
      provides: "Top-level Domain barrel re-exporting contracts, errors, results"
      min_lines: 3
  key_links:
    - from: "Errors/Specific/index.ts"
      to: "Errors/Specific/complaint-not-found.error.ts"
      via: "export"
      pattern: "ComplaintNotFoundError"
    - from: "Errors/index.ts"
      to: "Errors/domain-error.ts"
      via: "export"
      pattern: "DomainError"
    - from: "Errors/index.ts"
      to: "Errors/Specific/index.ts"
      via: "export"
      pattern: "Specific"
    - from: "Results/index.ts"
      to: "Results/domain-result.ts"
      via: "export"
      pattern: "DomainResult"
    - from: "Domain/index.ts"
      to: "Errors/index.ts"
      via: "export"
      pattern: "errors"
    - from: "Domain/index.ts"
      to: "Results/index.ts"
      via: "export"
      pattern: "results"
    - from: "Domain/index.ts"
      to: "Errors/domain-error.contract.ts"
      via: "export through contracts barrel"
      pattern: "contracts"
---

<objective>
Clean up old scaffold structure (delete ComplaintErrors/ and UserErrors/ subdirectories, remove unused stub files) and build the barrel chain for the Domain layer.

Purpose: Complete the Domain layer's export surface by collapsing the old subdirectory structure into flat Specific/ files (created in Plan 2), removing stub files that were explicitly skipped by context decisions (domain-result.contract.ts, domain-result.type-guard.ts, domain-result.validator.ts), and wiring up the barrel chain so downstream layers can import all domain types from `Domain/index.ts`.

Output: Barrel files at every level (Specific/index.ts, Errors/index.ts, Results/index.ts, Domain/index.ts) + deleted stale subdirectories and unused stub files.
</objective>

<execution_context>
@C:/Users/hesha/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hesha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/Core/Foundations/Domain/Errors/Specific/index.ts (currently empty — target file)
@src/Core/Foundations/Domain/Errors/index.ts (currently empty — target file)
@src/Core/Foundations/Domain/Results/index.ts (currently empty — target file)
@src/Core/Foundations/Domain/index.ts (currently empty — target file)

@src/Core/Foundations/Domain/Errors/domain-error.contract.ts
@src/Core/Foundations/Domain/Errors/domain-error.ts
@src/Core/Foundations/Domain/Errors/domain-error.validator.ts
@src/Core/Foundations/Domain/Errors/domain-error.type-guard.ts
@src/Core/Foundations/Domain/Errors/domain-error.type-guard.ts
@src/Core/Foundations/Domain/Errors/Specific/complaint-not-found.error.ts
@src/Core/Foundations/Domain/Errors/Specific/complaint-already-exists.error.ts
@src/Core/Foundations/Domain/Errors/Specific/user-not-found.error.ts
@src/Core/Foundations/Domain/Results/domain-result.ts

@src/Core/Foundations/Base/index.ts (reference barrel pattern)
@src/Core/Foundations/index.ts (reference barrel pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old subdirectories and unused stub files</name>
  <files>
    - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/complaint-not-found.error.ts
    - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/complaint-already-exists.error.ts
    - src/Core/Foundations/Domain/Errors/Specific/ComplaintErrors/index.ts
    - src/Core/Foundations/Domain/Errors/Specific/UserErrors/user-not-found.error.ts
    - src/Core/Foundations/Domain/Errors/Specific/UserErrors/index.ts
    - src/Core/Foundations/Domain/Results/domain-result.contract.ts
    - src/Core/Foundations/Domain/Results/domain-result.type-guard.ts
    - src/Core/Foundations/Domain/Results/domain-result.validator.ts
  </files>
  <read_first>
    - src/Core/Foundations/Domain/Errors/Specific/ (directory listing to verify old subdirectories exist)
    - src/Core/Foundations/Domain/Results/ (directory listing to verify unused stubs exist)
  </read_first>
  <action>
    Per D-10: Collapse existing `ComplaintErrors/` and `UserErrors/` subdirectories. The flat files already exist in Plan 2. Now delete the old subdirectory files.

    Delete stale files (the new flat files at `Specific/complaint-not-found.error.ts` etc. replace these):
    - `Specific/ComplaintErrors/complaint-not-found.error.ts`
    - `Specific/ComplaintErrors/complaint-already-exists.error.ts`
    - `Specific/ComplaintErrors/index.ts`
    - `Specific/UserErrors/user-not-found.error.ts`
    - `Specific/UserErrors/index.ts`

    After all files in each subdirectory are deleted, remove the directories themselves:
    - `Specific/ComplaintErrors/`
    - `Specific/UserErrors/`

    Per D-07: Delete the unused domain-result contract stub (no contract file needed per decision):
    - `Results/domain-result.contract.ts`

    Per D-14: Delete the unused domain-result type guard stub (no type guard needed):
    - `Results/domain-result.type-guard.ts`

    Per D-16: Delete the unused domain-result validator stub (DomainResult validated via inner DomainError):
    - `Results/domain-result.validator.ts`

    Use `Remove-Item -LiteralPath` for each file and directory. Check they exist before removing.

    Also check if `Results/domain-result.contract.ts`, `domain-result.type-guard.ts`, and `domain-result.validator.ts` are empty stubs (they should be, from the scaffold). If they contain any code, verify it's safe to delete.
  </action>
  <acceptance_criteria>
    - `Specific/ComplaintErrors/` directory and all its files no longer exist
    - `Specific/UserErrors/` directory and all its files no longer exist
    - `Results/domain-result.contract.ts` no longer exists
    - `Results/domain-result.type-guard.ts` no longer exists
    - `Results/domain-result.validator.ts` no longer exists
    - `npx tsc --noEmit` compiles without errors (no broken imports from deleted files)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Old subdirectories and unused stubs deleted, tsc compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create Specific/index.ts barrel</name>
  <files>
    - src/Core/Foundations/Domain/Errors/Specific/index.ts
  </files>
  <read_first>
    - src/Core/Foundations/Domain/Errors/Specific/index.ts (currently empty — target file)
  </read_first>
  <action>
    Per D-11: Create `Specific/index.ts` as a barrel file re-exporting all three specific errors.

    ```
    export { ComplaintNotFoundError } from "./complaint-not-found.error";
    export { ComplaintAlreadyExistsError } from "./complaint-already-exists.error";
    export { UserNotFoundError } from "./user-not-found.error";
    ```

    Order is alphabetical by export name. Use named exports (not `export *`) for explicit readability.
  </action>
  <acceptance_criteria>
    - `Specific/index.ts` re-exports `ComplaintNotFoundError`, `ComplaintAlreadyExistsError`, `UserNotFoundError`
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Specific/index.ts barrel compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Create Errors/index.ts barrel</name>
  <files>
    - src/Core/Foundations/Domain/Errors/index.ts
  </files>
  <read_first>
    - src/Core/Foundations/Domain/Errors/index.ts (currently empty — target file)
  </read_first>
  <action>
    Create `Errors/index.ts` as a barrel file re-exporting DomainError, the validator and type guard (for completeness), and the Specific subdirectory.

    ```
    export { DomainError } from "./domain-error";
    export { DomainErrorValidator } from "./domain-error.validator";
    export { isDomainError } from "./domain-error.type-guard";
    export * from "./Specific";
    ```

    Using `export * from "./Specific"` to pass through all specific error exports. Named exports for the main domain error components.

    Order: DomainError first (primary export), then DomainErrorValidator, then isDomainError, then Specific subdirectory.
  </action>
  <acceptance_criteria>
    - `Errors/index.ts` re-exports `DomainError`, `DomainErrorValidator`, `isDomainError`
    - `Errors/index.ts` re-exports `ComplaintNotFoundError`, `ComplaintAlreadyExistsError`, `UserNotFoundError` via `export * from "./Specific"`
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Errors/index.ts barrel compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 4: Create Results/index.ts barrel</name>
  <files>
    - src/Core/Foundations/Domain/Results/index.ts
  </files>
  <read_first>
    - src/Core/Foundations/Domain/Results/index.ts (currently empty — target file)
  </read_first>
  <action>
    Create `Results/index.ts` as a barrel file re-exporting the DomainResult type alias.

    ```
    export type { DomainResult } from "./domain-result";
    ```

    Since `DomainResult` is a type alias (not a class), use `export type` for explicit type-only re-export. This avoids the `isolatedModules` issue and stays consistent with TypeScript best practices.

    Note: `DomainResult` is the only export from this barrel. The contract, validator, and type guard stubs were deleted in Task 1 per D-07, D-14, D-16.
  </action>
  <acceptance_criteria>
    - `Results/index.ts` re-exports `DomainResult` type from `./domain-result`
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Results/index.ts barrel compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 5: Create Domain/index.ts barrel</name>
  <files>
    - src/Core/Foundations/Domain/index.ts
  </files>
  <read_first>
    - src/Core/Foundations/Domain/index.ts (currently empty — target file)
  </read_first>
  <action>
    Per D-17: Create `Domain/index.ts` re-exporting exactly three things:
    - `export * from "./Errors"` — errors barrel (DomainError, validator, type guard, specific errors)
    - `export * from "./Results"` — results barrel (DomainResult)
    - `export * from "./contracts"` — contracts barrel (DomainErrorContract)

    Per D-18: Domain layer does NOT re-export Base layer components. Consumers import Base and Domain separately (or via `Foundations/index.ts` in future phases).

    The barrel creates a `contracts/` subdirectory barrel. Since there is only one contract file (`domain-error.contract.ts`), create a `src/Core/Foundations/Domain/contracts/` directory with an `index.ts` that re-exports:

    ```
    // contracts/index.ts
    export type { DomainErrorContract } from "../Errors/domain-error.contract";
    ```

    Then `Domain/index.ts` becomes:
    ```
    export * from "./Errors";
    export * from "./Results";
    export * from "./contracts";
    ```

    Note: The `contracts/` directory does not yet exist under Domain. Create it with `New-Item -ItemType Directory` as needed.
  </action>
  <acceptance_criteria>
    - `Domain/contracts/` directory created with `index.ts` re-exporting `DomainErrorContract`
    - `Domain/index.ts` re-exports `./Errors`, `./Results`, `./contracts`
    - Importing from `Domain/index.ts` resolves all Domain layer exports
    - No Base layer re-exports from `Domain/index.ts`
    - `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Domain/index.ts barrel compiles cleanly with only contracts, errors, results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Pure barrel files and directory cleanup — no executable code, no data flow, no I/O.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Dom-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external dependencies — no package install needed |
</threat_model>

<verification>
All tasks must pass: `npx tsc --noEmit` compiles with no errors.
</verification>

<success_criteria>
- ComplaintErrors/ and UserErrors/ subdirectories deleted
- Unused stub files (domain-result.contract.ts, domain-result.type-guard.ts, domain-result.validator.ts) deleted
- Specific/index.ts re-exports all three specific errors
- Errors/index.ts re-exports DomainError + Specific errors + validator + type guard
- Results/index.ts re-exports DomainResult type alias
- Domain/contracts/index.ts re-exports DomainErrorContract
- Domain/index.ts re-exports contracts, errors, results — no Base re-exports
- `npx tsc --noEmit` passes
</success_criteria>

<output>
Create `.planning/phases/03-domain-layer/03-03-SUMMARY.md` when done
</output>
